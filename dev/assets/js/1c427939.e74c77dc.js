"use strict";(self.webpackChunkrtl_buddy_docs=self.webpackChunkrtl_buddy_docs||[]).push([[1725],{595(e,s,t){t.r(s),t.d(s,{assets:()=>h,contentTitle:()=>i,default:()=>l,frontMatter:()=>o,metadata:()=>n,toc:()=>d});const n=JSON.parse('{"id":"concepts/phys","title":"Physical Metrics","description":"Query saved synthesis and power artefacts by module and instance with rb phys or the hub\'s synth+power pane, from the physical model each run writes.","source":"@site/docs/concepts/phys.md","sourceDirName":"concepts","slug":"/concepts/phys","permalink":"/rtl_buddy/dev/concepts/phys","draft":false,"unlisted":false,"editUrl":"https://github.com/rtl-buddy/rtl_buddy/edit/main/docs/concepts/phys.md","tags":[],"version":"current","lastUpdatedBy":"Hern","lastUpdatedAt":1789959725000,"frontMatter":{"description":"Query saved synthesis and power artefacts by module and instance with rb phys or the hub\'s synth+power pane, from the physical model each run writes."},"sidebar":"docs","previous":{"title":"Power Analysis","permalink":"/rtl_buddy/dev/concepts/power"},"next":{"title":"FPGA Implementation","permalink":"/rtl_buddy/dev/concepts/fpga"}}');var r=t(4848),a=t(8453);const o={description:"Query saved synthesis and power artefacts by module and instance with rb phys or the hub's synth+power pane, from the physical model each run writes."},i="Physical Metrics",h={},d=[{value:"Read the model a run produced",id:"read-the-model-a-run-produced",level:2},{value:"List the runs a project holds",id:"list-the-runs-a-project-holds",level:2},{value:"Tell two runs apart",id:"tell-two-runs-apart",level:2},{value:"Read a model with only one half",id:"read-a-model-with-only-one-half",level:2},{value:"What the module join can answer",id:"what-the-module-join-can-answer",level:2},{value:"Roll up a hierarchy",id:"roll-up-a-hierarchy",level:2},{value:"Browse the model in the hub",id:"browse-the-model-in-the-hub",level:2},{value:"Machine payloads",id:"machine-payloads",level:2}];function c(e){const s={a:"a",code:"code",em:"em",h1:"h1",h2:"h2",header:"header",li:"li",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,a.R)(),...e.components};return(0,r.jsxs)(r.Fragment,{children:[(0,r.jsx)(s.header,{children:(0,r.jsx)(s.h1,{id:"physical-metrics",children:"Physical Metrics"})}),"\n",(0,r.jsxs)(s.p,{children:[(0,r.jsx)(s.code,{children:"rb phys"})," reads the physical model ",(0,r.jsx)(s.code,{children:"rb synth"})," and ",(0,r.jsx)(s.code,{children:"rb power"})," already write and answers per-module and per-instance questions from it. It runs no tools and writes nothing."]}),"\n",(0,r.jsx)(s.h2,{id:"read-the-model-a-run-produced",children:"Read the model a run produced"}),"\n",(0,r.jsxs)(s.p,{children:["Every synthesis and power run writes ",(0,r.jsx)(s.code,{children:"phys-model.json"})," and ",(0,r.jsx)(s.code,{children:"phys-manifest.json"})," into an artefact directory \u2014 its own, or the one a power run's ",(0,r.jsx)(s.code,{children:"phys-run:"})," names. Without an override, ",(0,r.jsx)(s.code,{children:"rb phys"})," reads the newest ",(0,r.jsx)(s.code,{children:"phys-manifest.json"})," under the project root."]}),"\n",(0,r.jsx)(s.pre,{children:(0,r.jsx)(s.code,{className:"language-bash",children:"rb phys summary\nrb phys summary --limit 0\nrb phys summary --limit 0 --instances-limit none\nrb phys module sub\nrb phys instance u_sub\nrb phys summary --phys-dir verif/blk/artefacts/nightly\n"})}),"\n",(0,r.jsxs)(s.ul,{children:["\n",(0,r.jsxs)(s.li,{children:[(0,r.jsx)(s.code,{children:"summary"})," reports the run header, the design totals, the heaviest modules by cell count, and the hottest instances by total power. ",(0,r.jsx)(s.code,{children:"--limit 0"})," shows every row of both rankings. ",(0,r.jsx)(s.code,{children:"--modules-limit"})," and ",(0,r.jsx)(s.code,{children:"--instances-limit"})," override ",(0,r.jsx)(s.code,{children:"--limit"})," for one ranking each, and take ",(0,r.jsx)(s.code,{children:"none"})," for a ranking you do not want at all \u2014 ",(0,r.jsx)(s.code,{children:"--limit 0 --instances-limit none"})," is the complete module table without a leaf instance row behind it, which on a mapped design is the difference between a few hundred rows and a few hundred thousand."]}),"\n",(0,r.jsxs)(s.li,{children:[(0,r.jsx)(s.code,{children:"module"})," reports one module's cells and area, then the instances of it and the power they burn. The name may be a design module from the synthesis half or a Liberty cell from the power half; see ",(0,r.jsx)(s.a,{href:"#what-the-module-join-can-answer",children:"What the module join can answer"}),"."]}),"\n",(0,r.jsxs)(s.li,{children:[(0,r.jsx)(s.code,{children:"instance"})," reports one instance's power. A path that names a subtree instead of a leaf lists the leaves under it, hottest by total power first, and rolls them up. Paths are compared level by level, so ",(0,r.jsx)(s.code,{children:"u_sub.u_leaf"})," and ",(0,r.jsx)(s.code,{children:"u_sub/u_leaf"})," name the same instance whichever spelling the tool wrote."]}),"\n"]}),"\n",(0,r.jsxs)(s.p,{children:[(0,r.jsx)(s.code,{children:"--phys-dir"})," selects an artefact directory and ",(0,r.jsx)(s.code,{children:"--manifest"})," names the document directly. An explicit ",(0,r.jsx)(s.code,{children:"--manifest"})," wins over ",(0,r.jsx)(s.code,{children:"--phys-dir"}),", which wins over discovery."]}),"\n",(0,r.jsxs)(s.p,{children:["An unknown module or instance exits 2 and reports close candidates. A project with no manifest exits 2 naming ",(0,r.jsx)(s.code,{children:"rb synth"})," and ",(0,r.jsx)(s.code,{children:"rb power"}),"."]}),"\n",(0,r.jsx)(s.h2,{id:"list-the-runs-a-project-holds",children:"List the runs a project holds"}),"\n",(0,r.jsxs)(s.p,{children:["A project accumulates one artefact directory per partition, per corner and per power mode. ",(0,r.jsx)(s.code,{children:"rb phys runs"})," lists them, newest first:"]}),"\n",(0,r.jsx)(s.pre,{children:(0,r.jsx)(s.code,{className:"language-bash",children:"rb phys runs\nrb phys runs --limit 0\n"})}),"\n",(0,r.jsxs)(s.p,{children:["Each row carries the run name and top, the backends that produced it, the power mode and what drove the switching, a fingerprint of the configuration that shaped the netlist, the ",(0,r.jsx)(s.code,{children:"rb xplr"})," experiment id when the run sits under one, and when it was generated. The row marked ",(0,r.jsx)(s.code,{children:"*"})," is the newest \u2014 the run the other verbs read without ",(0,r.jsx)(s.code,{children:"--phys-dir"}),". The artefact directories are printed under the table rather than in it, one per run, because that is the value the next command takes:"]}),"\n",(0,r.jsx)(s.pre,{children:(0,r.jsx)(s.code,{className:"language-bash",children:"rb phys summary --phys-dir verif/blk/artefacts/nightly\n"})}),"\n",(0,r.jsx)(s.p,{children:"The listing reads one manifest per run and opens no model, so it stays cheap on a project with many. A manifest that cannot be read is listed with its error rather than dropped: discovery found the file, and a menu that omitted it would report fewer runs than the project has. A project with no physical artefacts lists nothing and exits 0, unlike the other three verbs \u2014 they were asked about a run, and this one is asking what runs there are."}),"\n",(0,r.jsx)(s.h2,{id:"tell-two-runs-apart",children:"Tell two runs apart"}),"\n",(0,r.jsxs)(s.p,{children:["Both producers record what ",(0,r.jsx)(s.em,{children:"shaped"})," a run beside what it measured, so two runs of one design are two measurements rather than one document read twice."]}),"\n",(0,r.jsxs)(s.p,{children:[(0,r.jsx)(s.code,{children:"rb power"})," records its ",(0,r.jsx)(s.strong,{children:"mode"})," \u2014 ",(0,r.jsx)(s.code,{children:"static"})," or ",(0,r.jsx)(s.code,{children:"dynamic"})," \u2014 and a compact ",(0,r.jsx)(s.strong,{children:"activity"})," block: the resolved source (",(0,r.jsx)(s.code,{children:"default"}),", ",(0,r.jsx)(s.code,{children:"synthetic"}),", ",(0,r.jsx)(s.code,{children:"saif"})," or ",(0,r.jsx)(s.code,{children:"vcd"}),"), the trace and its sha256, the test whose artefact directory the trace came out of, the scope, and the toggle and duty a synthetic run used. Without them a \xb5W figure carries no statement of what it measures: leakage plus internal and a SAIF-driven total print in the same column. Each detail belongs to the source that consumed it: a ",(0,r.jsx)(s.code,{children:"saif"})," run records no toggle and duty, because a trace is what drove it, and a ",(0,r.jsx)(s.code,{children:"default"})," or ",(0,r.jsx)(s.code,{children:"synthetic"})," run records no trace, hash, test or scope even when its config still carries ",(0,r.jsx)(s.code,{children:"activity.saif"})," and ",(0,r.jsx)(s.code,{children:"activity.scope"}),", because the analysis read neither. The test name is derived from the layout \u2014 ",(0,r.jsx)(s.code,{children:"verif/demo/artefacts/csr_smoke/dump.saif"})," was produced by ",(0,r.jsx)(s.code,{children:"csr_smoke"})," \u2014 and is absent for a trace kept anywhere else. The sha256 identifies the trace by its bytes: a ",(0,r.jsx)(s.code,{children:"dump.saif"})," is rewritten in place by the next run of the test behind it, so a re-captured trace is a different measurement under an unchanged path. It is part of the run's identity and not of its label \u2014 a label is a table cell \u2014 so two runs against two versions of one trace read alike in the ",(0,r.jsx)(s.code,{children:"activity"})," column and apart in the block behind it."]}),"\n",(0,r.jsxs)(s.p,{children:["Both flows record a ",(0,r.jsx)(s.strong,{children:"config fingerprint"}),": the platform, the effort, the constraints file and its sha256, and a short digest of the effective tool options. The digest covers what the backend's generated script actually consumes, and nothing else, so a field the script never reads is left out rather than reporting a difference the netlist cannot have. A ",(0,r.jsx)(s.strong,{children:"Yosys synthesis"})," feeds the elaboration settings (frontend, the two correctness-gate modes, and under ",(0,r.jsx)(s.code,{children:"slang"})," the plugin path, ",(0,r.jsx)(s.code,{children:"single-unit"})," and ",(0,r.jsx)(s.code,{children:"best-effort-hierarchy"}),"), the resolved ",(0,r.jsx)(s.code,{children:"synth-args"}),", the parameters and defines, and the branch it took: a mapped run adds the ABC delay target read out of the SDC and the resolved Liberty list, and drops ",(0,r.jsx)(s.code,{children:"abc-args"}),", which its hard-coded ABC script ignores, while an unmapped run does the reverse. An ",(0,r.jsx)(s.strong,{children:"OpenROAD synthesis"})," feeds the same elaboration settings with ",(0,r.jsx)(s.code,{children:"synth-args"})," taken from the effort \u2014 that is where its stage-1 script takes it from \u2014 the resolved Liberty list, which both its stages read, and from stage 2 the command ",(0,r.jsx)(s.code,{children:"strategy"})," maps to, the sha256 of the effort's ",(0,r.jsx)(s.code,{children:"pre-sta-tcl"}),", and the resolved LEF list. Both library lists are the resolved ones rather than the platform name, because a config's own ",(0,r.jsx)(s.code,{children:"lib-paths"})," and ",(0,r.jsx)(s.code,{children:"lef-paths"})," are appended to the platform's and are the whole of the list when there is no platform. A ",(0,r.jsx)(s.strong,{children:"power run"})," feeds the tool, netlist source, the identity of the upstream run it read, the resolved technology its script reads, mode, activity source and register level; its ",(0,r.jsx)(s.code,{children:"tool_overrides"})," is left out because no power backend reads it. The technology is the Liberty and the one or two LEFs the generated Tcl names, in script order, for the reason the synthesis library lists are resolved rather than named by platform: a ",(0,r.jsx)(s.code,{children:"cfg-pnr-platforms"})," entry repointed at another corner is the same name. The upstream identity is what ",(0,r.jsx)(s.code,{children:"netlist_source"})," alone does not say \u2014 which synthesis or which place-and-route, not merely which kind: a ",(0,r.jsx)(s.code,{children:"netlist-source: synth"})," run records the sha256 of the netlist it measured and a ",(0,r.jsx)(s.code,{children:"netlist-source: pnr"})," run the project-relative path of the routed database it read. The constraints are the SDC the run actually read: a ",(0,r.jsx)(s.code,{children:"netlist-source: pnr"})," run with no explicit ",(0,r.jsx)(s.code,{children:"constraints:"})," records the router's ",(0,r.jsx)(s.code,{children:"<top>.routed.sdc"})," it was given. The constraints sha256, and the activity trace's, are taken before the tool is launched and re-checked when it returns, because a digest computed at publication would identify a file replaced while the run worked \u2014 a routed SDC a concurrent ",(0,r.jsx)(s.code,{children:"rb pnr"})," rewrote, a trace the next run of its test re-captured \u2014 and record it as what the run measured. A file that moved underneath the run records ",(0,r.jsx)(s.code,{children:"null"})," and logs a warning. It is taken over the ",(0,r.jsx)(s.em,{children:"effective values"}),", rendered as canonical JSON, not over the files they came from, so two configs that spell one setting differently and resolve to the same options are one experiment. As a summary it reads ",(0,r.jsx)(s.code,{children:"nangate45 \xb7 timing-opt \xb7 sdc a1b2c3d4 \xb7 opts 5655beea1f20"}),"."]}),"\n",(0,r.jsxs)(s.p,{children:["A manifest under ",(0,r.jsx)(s.code,{children:"artefacts/xplr/<exp-id>/"})," carries that ",(0,r.jsx)(s.strong,{children:"experiment id"})," in every payload's run header and in the run listing. The id comes from the path, so it is there before the experiment's ",(0,r.jsx)(s.code,{children:"record.json"})," exists; the record is read only for the experiment's ",(0,r.jsx)(s.code,{children:"hypothesis"}),", which becomes the entry's label. A malformed record costs the label and nothing else. A run inside a materialized checkout is labelled too: ",(0,r.jsx)(s.code,{children:"rb xplr materialize"})," puts the worktree at ",(0,r.jsx)(s.code,{children:"artefacts/xplr/worktrees/<exp-id>/"}),", so the id is one level further down and the reproducible runs \u2014 the ones checked out at a pinned sha \u2014 are exactly the ones that must not lose it. The ",(0,r.jsx)(s.code,{children:"worktrees/"})," directory is not itself an experiment, and a worktree sidecar naming another location says this checkout is not that experiment's."]}),"\n",(0,r.jsxs)(s.p,{children:["None of this gates the merge. The netlist sha256 both producers record is what decides whether two halves describe one design, and it is the stronger test in both directions: the same options can produce two netlists, and two option sets can produce one. That gate is also what keeps same-top experiments from cross-pollinating a merged model \u2014 a power half only pairs with the synthesis whose bytes it measured. A power run's ",(0,r.jsx)(s.code,{children:"phys-run:"})," chooses which directory the two halves are asked to meet in and changes nothing about the gate; when the gate then refuses, that run says so rather than publishing a half-filled model quietly."]}),"\n",(0,r.jsx)(s.p,{children:"The keys are additive. A document written by an earlier rtl-buddy carries none of them, and every reader reports them as not recorded rather than refusing the document."}),"\n",(0,r.jsx)(s.h2,{id:"read-a-model-with-only-one-half",children:"Read a model with only one half"}),"\n",(0,r.jsxs)(s.p,{children:["A synthesis fills the model's ",(0,r.jsx)(s.code,{children:"modules"})," half and a power run fills its ",(0,r.jsx)(s.code,{children:"instances"})," half. The two meet in one artefact directory, for the same top, and produce a complete model in either order. Which directory that is is the power run's to say: by default each run publishes into its own, so the halves meet only where a power run is named after the synthesis it reads and configured in the same directory, and ",(0,r.jsx)(s.code,{children:"phys-run: <synth run>"})," in ",(0,r.jsx)(s.code,{children:"power.yaml"})," names the synthesis run to publish beside instead. See ",(0,r.jsx)(s.a,{href:"/rtl_buddy/dev/concepts/power#pair-the-model-with-a-synthesis-run",children:"Pair the model with a synthesis run"}),", ",(0,r.jsx)(s.a,{href:"/rtl_buddy/dev/concepts/synthesis#inspect-artefacts",children:"Synthesis"})," and ",(0,r.jsx)(s.a,{href:"/rtl_buddy/dev/concepts/power#inspect-artefacts",children:"Power Analysis"}),"."]}),"\n",(0,r.jsxs)(s.p,{children:["With one half absent, every verb still answers from the half that is present and says which command produces the other \u2014 either because the other command has not run yet, because it published somewhere else, or because it could not merge with what is already here. The merge is gated on the netlist hash both producers record, so a power half taken from a routed database (",(0,r.jsx)(s.code,{children:"netlist-source: pnr"}),", which has no netlist to hash) cannot be paired with: a later ",(0,r.jsx)(s.code,{children:"rb synth"})," into that directory would ",(0,r.jsx)(s.em,{children:"replace"})," the model rather than complete it. The note says so, and names what does work \u2014 synthesise, then re-run ",(0,r.jsx)(s.code,{children:"rb power"})," on the netlist the synthesis wrote, so both halves measure the same one. ",(0,r.jsx)(s.code,{children:"rb phys instance"})," is the exception: instance rows exist only in the power half, so it exits 2 pointing at ",(0,r.jsx)(s.code,{children:"rb power"}),"."]}),"\n",(0,r.jsx)(s.h2,{id:"what-the-module-join-can-answer",children:"What the module join can answer"}),"\n",(0,r.jsxs)(s.p,{children:["The two halves spell ",(0,r.jsx)(s.code,{children:"module"})," in two namespaces. In the synthesis half it is an RTL module name, as Yosys' ",(0,r.jsx)(s.code,{children:"stat"})," saw it. In the power half it is the Liberty cell each leaf instance is an instance of \u2014 ",(0,r.jsx)(s.code,{children:"DFF_X1"}),", ",(0,r.jsx)(s.code,{children:"NAND2_X1"})," \u2014 because a mapped netlist's leaves are cells, not RTL modules."]}),"\n",(0,r.jsxs)(s.p,{children:["So ",(0,r.jsx)(s.code,{children:"rb phys module"})," answers Liberty-cell questions: ",(0,r.jsx)(s.code,{children:"rb phys module DFF_X1"})," reports every flop instance and the power they burn together."]}),"\n",(0,r.jsxs)(s.p,{children:["It does not attribute power to an RTL module, and flattening the design does not change that. The join matches the power half's ",(0,r.jsx)(s.code,{children:"module"})," field as it stands, and that field holds the cell a leaf is an instance of \u2014 so no leaf row carries ",(0,r.jsx)(s.code,{children:"u_cpu"}),"'s name, or the top's, and the join finds nothing. When a name resolves out of the synthesis half alone and the power half is populated, the payload's ",(0,r.jsx)(s.code,{children:"instance_join"}),' says so in words and the console prints it, so an empty instance list is never mistaken for "this block burns nothing". Ask what a block burns by its instance path instead: ',(0,r.jsx)(s.code,{children:"rb phys instance u_cpu"})," sums the leaf rows under it \u2014 see ",(0,r.jsx)(s.a,{href:"#roll-up-a-hierarchy",children:"Roll up a hierarchy"}),"."]}),"\n",(0,r.jsxs)(s.p,{children:["Nothing stops one name from being in both namespaces \u2014 a Liberty cell named after a block, or an RTL module called ",(0,r.jsx)(s.code,{children:"DFF_X1"}),". Then the two halves are measuring two different things under one word, and ",(0,r.jsx)(s.code,{children:"rb phys module"})," says so: ",(0,r.jsx)(s.code,{children:"namespaces"})," lists both and ",(0,r.jsx)(s.code,{children:"instance_join"})," carries a collision note the console prints. The row and the instances are still reported, and still not added together; the note is what keeps a module's cells and area beside a cell type's power from reading as one block's totals."]}),"\n",(0,r.jsxs)(s.p,{children:["See ",(0,r.jsx)(s.a,{href:"/rtl_buddy/dev/known-issues#rb-phys-module-reports-no-power-for-an-rtl-module",children:"Known Issues"}),"."]}),"\n",(0,r.jsx)(s.h2,{id:"roll-up-a-hierarchy",children:"Roll up a hierarchy"}),"\n",(0,r.jsxs)(s.p,{children:["The model records leaf values only, because a subtree sum depends on the hierarchy the consumer projects onto. ",(0,r.jsx)(s.code,{children:"rb phys instance <path>"})," is that consumer: it sums the leaves under the path at query time and leaves the document unchanged."]}),"\n",(0,r.jsxs)(s.p,{children:["A path that names a row exactly is answered by that row. ",(0,r.jsx)(s.code,{children:"match"})," reports which question was answered \u2014 ",(0,r.jsx)(s.code,{children:"exact"})," or ",(0,r.jsx)(s.code,{children:"prefix"})," \u2014 and the rollup answers that question and no other: the named row for an exact match, every leaf under the path for a prefix one. A path that is both a row and a prefix of other rows therefore rolls up to the row alone; the rows below it are still listed and still counted by ",(0,r.jsx)(s.code,{children:"child_count"}),", and the console says they are there for navigation rather than in the total."]}),"\n",(0,r.jsxs)(s.p,{children:["The rollup adds the four power columns directly, and reports nothing else. It carries no area: the model has no per-cell area to sum, and the only substitute available \u2014 joining each leaf's module to the synthesis half \u2014 would add the whole module's area once per leaf, on a name that may belong to the other namespace entirely. Area is reported per RTL module, by ",(0,r.jsx)(s.code,{children:"rb phys module"}),", against the name Yosys counted it for."]}),"\n",(0,r.jsx)(s.h2,{id:"browse-the-model-in-the-hub",children:"Browse the model in the hub"}),"\n",(0,r.jsxs)(s.p,{children:["The hub serves the same model as a page. Start the browser layer and open ",(0,r.jsx)(s.code,{children:"/phy"}),":"]}),"\n",(0,r.jsx)(s.pre,{children:(0,r.jsx)(s.code,{className:"language-bash",children:"rb hub start --serve-viewer\n"})}),"\n",(0,r.jsxs)(s.p,{children:[(0,r.jsx)(s.code,{children:"GET /phy.json"})," is the ",(0,r.jsx)(s.code,{children:"rb phys summary"})," payload with no row limit, so the pane and the CLI cannot disagree about a number. It also carries the ",(0,r.jsx)(s.code,{children:"rb phys runs"})," listing, which the pane renders as a run dropdown in its header: entries read ",(0,r.jsx)(s.code,{children:"run \xb7 top \xb7 backends \xb7 mode (activity) \xb7 experiment"}),", mark the run being shown and the newest, and carry the artefact directory and the config fingerprint on the hover. Choosing one re-fetches ",(0,r.jsx)(s.code,{children:"GET /phy.json?dir=<phys dir>"}),"; the newest stays the default for a pane nobody has touched, and choosing the newest entry is how you go back to following it rather than pinning the run that happens to be newest today. An inbound ",(0,r.jsx)(s.code,{children:"phys-focus"})," applies to the run the pane is showing \u2014 the message names a target and a metric, never a run, so switching runs is a gesture in the pane rather than something another app can do to it. The pane ranks modules by cells or area and instances by leakage, dynamic or total power, tints each ranked column, and filters the instance table to one module when you click it."]}),"\n",(0,r.jsxs)(s.p,{children:["The pane holds every row, but renders the instance table 500 at a time with a ",(0,r.jsx)(s.code,{children:"show more"})," / ",(0,r.jsx)(s.code,{children:"show all"})," control under it \u2014 a mapped design's power half runs to six figures of leaf instances, and a table that rebuilt all of them on every sort click would freeze the tab. Selecting a row that ranks below the window moves the window to it, keeping the row and its neighbours in the ranking on screen without lifting the bound; the control then says how many rows sit above and below the slice. Sorting, filtering, the tints and the totals are computed over the whole set regardless of what is on screen."]}),"\n",(0,r.jsxs)(s.p,{children:[(0,r.jsx)(s.code,{children:"dynamic"})," is internal plus switching, summed in the browser rather than stored: no producer writes that column. The totals header shows the flow's own scraped total beside the sum of the rows, and says when they disagree. The synthesis scrape is anchored to the top module, so a disagreement is a real difference between the two measurements rather than a submodule's area standing in for the design's (rtl-buddy/rtl_buddy#559)."]}),"\n",(0,r.jsx)(s.p,{children:"Point the pane at a target from anywhere:"}),"\n",(0,r.jsx)(s.pre,{children:(0,r.jsx)(s.code,{className:"language-bash",children:"rb hub send phys-focus module:sub --metric area\nrb hub send phys-focus instance:u_sub/_64_\n"})}),"\n",(0,r.jsxs)(s.p,{children:["An inbound focus is always visible: when the search box would hide the row it selects, the pane takes the search off and says so in its status line, and leaves it alone when the target already matches it. An unprefixed target is read as an instance path. The pane resolves exact leaf rows, so an instance target has to name a row: a subtree path \u2014 the one ",(0,r.jsx)(s.code,{children:"rb phys instance"})," answers with ",(0,r.jsx)(s.code,{children:"match: prefix"})," \u2014 selects nothing, and one of its children is what to send instead. The hub replays the latest focus to the pane when it registers, so sending one before the tab is open works \u2014 as does a selection the schematic broadcast before the pane's model had loaded. Clicking a module in the pane broadcasts ",(0,r.jsx)(s.code,{children:"graph_focus"})," and clicking an instance broadcasts ",(0,r.jsx)(s.code,{children:"selection_changed"}),", which the schematic follows. The pane roots the path it sends at the design top, since that is the schematic's coordinate and a model row is always relative to the top; on the way back in it resolves a path against the rows themselves, reading it both as sent and with a leading top level removed, so a design whose top name is also an instance name still selects the right row. The tables keep whatever spelling the model recorded. That top is the physical model's own, and the pane cannot know which view the schematic is displaying: a ",(0,r.jsx)(s.code,{children:"/sch"})," showing a testbench around the DUT, or another design entirely, is sent a path that names no instance there and selects nothing. An instance path only means something inside the hierarchy the model recorded it in, so pair the pane with a schematic of the same design; see ",(0,r.jsx)(s.a,{href:"/rtl_buddy/dev/known-issues#phys-pane-and-schematic-selections-cross-only-within-one-hierarchy",children:"Known Issues"}),". See ",(0,r.jsx)(s.a,{href:"/rtl_buddy/dev/concepts/hub#synthpower-pane",children:"Hub"})," for the routes and the peer contract."]}),"\n",(0,r.jsxs)(s.p,{children:["Clicking a module filters the instance table to it. When the name is an RTL module and every leaf carries a Liberty cell name, nothing matches, and the pane says so rather than showing an empty table. When the name is in both namespaces the lens looks complete instead \u2014 a cell type's leaves listed under a module's cells and area \u2014 so the pane prints the collision note there too, saying which measurement is whose. Both are the calls ",(0,r.jsx)(s.code,{children:"rb phys module"})," makes on its own payload; see ",(0,r.jsx)(s.a,{href:"#what-the-module-join-can-answer",children:"What the module join can answer"}),"."]}),"\n",(0,r.jsxs)(s.p,{children:["The same model also paints the design-knowledge-graph pane. Open ",(0,r.jsx)(s.code,{children:"/gph"}),", tick ",(0,r.jsx)(s.code,{children:"heat"}),", and its module nodes are filled with this model's numbers: cells and area joined from the synthesis half by module name \u2014 the module definition's, counted once \u2014 and the power of every leaf row inside every instantiation of the module, summed, because the power half's ",(0,r.jsx)(s.code,{children:"module"})," column is a Liberty cell and the join has to be by instance path. The pane prints how many instantiations and how many rows went into each figure, since the two halves do not count over the same thing. Its metric switcher and its run dropdown are this pane's, over the same ",(0,r.jsx)(s.code,{children:"GET /phy.json?dir=<phys dir>"}),", so a run chosen in either is the same run, and a ",(0,r.jsx)(s.code,{children:"phys-focus"})," turns that pane's overlay on as well. See ",(0,r.jsx)(s.a,{href:"/rtl_buddy/dev/concepts/graph#physical-heat-on-the-graph",children:"Physical Heat on the Graph"}),"."]}),"\n",(0,r.jsx)(s.h2,{id:"machine-payloads",children:"Machine payloads"}),"\n",(0,r.jsxs)(s.p,{children:[(0,r.jsx)(s.code,{children:"--machine"})," emits the payload the verb built, carrying its own ",(0,r.jsx)(s.code,{children:"schema_version"}),", the project-relative manifest and model paths, the run header, the artefact block, and the verb's data: rankings for ",(0,r.jsx)(s.code,{children:"summary"}),", the module row plus its instances for ",(0,r.jsx)(s.code,{children:"module"}),", and the row or subtree plus its rollup for ",(0,r.jsx)(s.code,{children:"instance"}),"."]}),"\n",(0,r.jsxs)(s.p,{children:["Every run header also carries the run's identity: ",(0,r.jsx)(s.code,{children:"power_mode"}),", ",(0,r.jsx)(s.code,{children:"power_activity"})," (the recorded block plus a derived ",(0,r.jsx)(s.code,{children:"label"}),"), ",(0,r.jsx)(s.code,{children:"config"})," with a ",(0,r.jsx)(s.code,{children:"synth"})," and a ",(0,r.jsx)(s.code,{children:"power"})," fingerprint (each plus a derived ",(0,r.jsx)(s.code,{children:"summary"}),"), and ",(0,r.jsx)(s.code,{children:"xplr"})," \u2014 ",(0,r.jsx)(s.code,{children:'{"id", "label"}'})," or ",(0,r.jsx)(s.code,{children:"null"}),". Each half's ",(0,r.jsx)(s.code,{children:"halves"})," entry echoes ",(0,r.jsx)(s.code,{children:"mode"})," and ",(0,r.jsx)(s.code,{children:"activity"}),", ",(0,r.jsx)(s.code,{children:"null"})," on the synthesis half where neither exists."]}),"\n",(0,r.jsxs)(s.p,{children:[(0,r.jsx)(s.code,{children:"rb phys runs --machine"})," emits its own payload: ",(0,r.jsx)(s.code,{children:"count"}),", the applied ",(0,r.jsx)(s.code,{children:"limit"}),", and ",(0,r.jsx)(s.code,{children:"runs"}),", newest first. Each entry carries ",(0,r.jsx)(s.code,{children:"phys_dir"})," and ",(0,r.jsx)(s.code,{children:"manifest"}),", ",(0,r.jsx)(s.code,{children:"run"}),", ",(0,r.jsx)(s.code,{children:"top"}),", ",(0,r.jsx)(s.code,{children:"run_command"}),", ",(0,r.jsx)(s.code,{children:"generated_at"}),", ",(0,r.jsx)(s.code,{children:"backends"}),", ",(0,r.jsx)(s.code,{children:"mode"})," and ",(0,r.jsx)(s.code,{children:"activity"}),", ",(0,r.jsx)(s.code,{children:"config"}),", a derived ",(0,r.jsx)(s.code,{children:"fingerprint"}),", ",(0,r.jsx)(s.code,{children:"xplr"}),", ",(0,r.jsx)(s.code,{children:"newest"}),", and ",(0,r.jsx)(s.code,{children:"error"})," \u2014 non-null only for a manifest that could not be read, where the rest of the entry is null and ",(0,r.jsx)(s.code,{children:"phys_dir"})," still selects the run. ",(0,r.jsx)(s.code,{children:"phys_dir"})," is derived from where the manifest was found, not from the document, so it is always relative to the root the listing was taken under."]}),"\n",(0,r.jsxs)(s.p,{children:[(0,r.jsx)(s.code,{children:"--limit"})," truncates the machine payload as well as the table, so a payload is exactly the rows the flag asked for and ",(0,r.jsx)(s.code,{children:"--limit 0"})," is how an agent asks for all of them. On ",(0,r.jsx)(s.code,{children:"summary"})," the per-ranking overrides do the same: the payload's ",(0,r.jsx)(s.code,{children:"limits"})," block reports what each ranking was actually headed at \u2014 a number, or ",(0,r.jsx)(s.code,{children:'"none"'})," for one that was suppressed \u2014 beside the ",(0,r.jsx)(s.code,{children:"limit"})," both would have followed. A suppressed ranking is an empty list, and ",(0,r.jsx)(s.code,{children:"counts"})," still reports how many rows the model holds, so it is never mistaken for a half the run did not produce; that half is ",(0,r.jsx)(s.code,{children:"counts"})," ",(0,r.jsx)(s.code,{children:"null"}),". Nothing is sorted or serialised for a ranking asked for as ",(0,r.jsx)(s.code,{children:"none"}),". A truncated list says so: ",(0,r.jsx)(s.code,{children:"summary"})," carries ",(0,r.jsx)(s.code,{children:"limit"})," beside its two rankings, ",(0,r.jsx)(s.code,{children:"module"})," carries ",(0,r.jsx)(s.code,{children:"limit"})," and ",(0,r.jsx)(s.code,{children:"instance_count"}),", and ",(0,r.jsx)(s.code,{children:"instance"})," carries ",(0,r.jsx)(s.code,{children:"limit"})," and ",(0,r.jsx)(s.code,{children:"child_count"})," \u2014 the counts are how many rows there are, never how many are listed. The figures beside a truncated list are still whole-set figures: ",(0,r.jsx)(s.code,{children:"module"}),"'s ",(0,r.jsx)(s.code,{children:"power"})," sums every instance of the module and a prefix ",(0,r.jsx)(s.code,{children:"instance"}),"'s ",(0,r.jsx)(s.code,{children:"rollup"})," sums every leaf under the path. The builders themselves default to the complete list, so only a caller that asks for a head gets one."]}),"\n",(0,r.jsxs)(s.p,{children:["Each payload also carries ",(0,r.jsx)(s.code,{children:"halves"})," and ",(0,r.jsx)(s.code,{children:"missing_halves"}),", which report which halves the model has and which command fills each one. Each half's entry also carries ",(0,r.jsx)(s.code,{children:"netlist_hash"}),": whether that half's producer recorded the hash of the netlist it measured, which is what the merge is gated on and therefore what a surface needs before advising anyone to run the other command. A ",(0,r.jsx)(s.code,{children:"null"})," value means the run did not measure it; ",(0,r.jsx)(s.code,{children:"0"})," means it measured zero. The ",(0,r.jsx)(s.code,{children:"module"})," payload also carries ",(0,r.jsx)(s.code,{children:"namespaces"})," \u2014 ",(0,r.jsx)(s.code,{children:'["rtl"]'}),", ",(0,r.jsx)(s.code,{children:'["liberty"]'}),", or both \u2014 and ",(0,r.jsx)(s.code,{children:"instance_join"}),": ",(0,r.jsx)(s.code,{children:"null"})," when the instance list needs no qualification, a sentence naming the Liberty-cell namespace limit when the module matched nothing because of it, and a collision sentence when the name is in both namespaces."]}),"\n",(0,r.jsxs)(s.p,{children:["A manifest or model whose JSON root is not an object \u2014 an empty file rebuilt by hand, a truncated write, a path pointed at the wrong file \u2014 is refused by name, as an unreadable one is, and so is one whose blocks are not the shapes the read dereferences them as: the producer blocks and the totals are objects, the manifest's ",(0,r.jsx)(s.code,{children:"model"})," and ",(0,r.jsx)(s.code,{children:"phys_dir"})," paths strings, the model's two halves arrays of objects. A ",(0,r.jsx)(s.code,{children:"null"})," block is not malformed, since that is what a half-filled run writes. Under ",(0,r.jsx)(s.code,{children:"--machine"})," either arrives as the usual error envelope rather than a traceback."]}),"\n",(0,r.jsxs)(s.p,{children:[(0,r.jsx)(s.code,{children:"rb mcp"})," exposes the same query builders as ",(0,r.jsx)(s.code,{children:"phys_runs"}),", ",(0,r.jsx)(s.code,{children:"phys_summary"}),", ",(0,r.jsx)(s.code,{children:"phys_module"}),", and ",(0,r.jsx)(s.code,{children:"phys_instance"}),". ",(0,r.jsx)(s.code,{children:"phys_runs"})," is where the ",(0,r.jsx)(s.code,{children:"phys_dir"})," the other three take comes from; it needs no arguments and takes the same ",(0,r.jsx)(s.code,{children:"limit"}),". They read files directly, run no EDA tool, and do not require a running hub. ",(0,r.jsx)(s.code,{children:"phys_dir"})," and ",(0,r.jsx)(s.code,{children:"manifest"})," are the tool arguments for ",(0,r.jsx)(s.code,{children:"--phys-dir"})," and ",(0,r.jsx)(s.code,{children:"--manifest"}),"; a relative path resolves against the project root. All three take the same ",(0,r.jsx)(s.code,{children:"limit"})," the flag does, with the same default: the head of the ranking, and ",(0,r.jsx)(s.code,{children:"0"})," for every row. ",(0,r.jsx)(s.code,{children:"phys_summary"})," also takes ",(0,r.jsx)(s.code,{children:"modules_limit"})," and ",(0,r.jsx)(s.code,{children:"instances_limit"}),", the tool arguments for ",(0,r.jsx)(s.code,{children:"--modules-limit"})," and ",(0,r.jsx)(s.code,{children:"--instances-limit"}),": an integer or the string ",(0,r.jsx)(s.code,{children:'"none"'}),", overriding ",(0,r.jsx)(s.code,{children:"limit"})," for one ranking each, and omitted means the ranking follows ",(0,r.jsx)(s.code,{children:"limit"}),". The payload says which it gave you \u2014 the applied ",(0,r.jsx)(s.code,{children:"limit"})," sits beside the untruncated ",(0,r.jsx)(s.code,{children:"instance_count"}),"/",(0,r.jsx)(s.code,{children:"child_count"}),", and the ",(0,r.jsx)(s.code,{children:"power"})," and ",(0,r.jsx)(s.code,{children:"rollup"})," sums cover every matching row either way. A ",(0,r.jsx)(s.code,{children:"phys_focus"})," tool mirroring ",(0,r.jsx)(s.code,{children:"rb hub send phys-focus"})," joins them when a live hub is discovered. See ",(0,r.jsx)(s.a,{href:"/rtl_buddy/dev/concepts/graph#the-mcp-server",children:"The MCP server"}),"."]})]})}function l(e={}){const{wrapper:s}={...(0,a.R)(),...e.components};return s?(0,r.jsx)(s,{...e,children:(0,r.jsx)(c,{...e})}):c(e)}},8453(e,s,t){t.d(s,{R:()=>o,x:()=>i});var n=t(6540);const r={},a=n.createContext(r);function o(e){const s=n.useContext(a);return n.useMemo(function(){return"function"==typeof e?e(s):{...s,...e}},[s,e])}function i(e){let s;return s=e.disableParentContext?"function"==typeof e.components?e.components(r):e.components||r:o(e.components),n.createElement(a.Provider,{value:s},e.children)}}}]);
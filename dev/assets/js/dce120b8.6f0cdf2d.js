"use strict";(self.webpackChunkrtl_buddy_docs=self.webpackChunkrtl_buddy_docs||[]).push([[7302],{6421(e,t,s){s.r(t),s.d(t,{assets:()=>d,contentTitle:()=>a,default:()=>h,frontMatter:()=>o,metadata:()=>n,toc:()=>c});const n=JSON.parse('{"id":"known-issues","title":"Quirks & Known Issues","description":"Current rtl_buddy limitations, surprising behavior, and required workarounds by workflow.","source":"@site/.docusaurus-historical-docs/dev/docs/known-issues.md","sourceDirName":".","slug":"/known-issues","permalink":"/rtl_buddy/dev/known-issues","draft":false,"unlisted":false,"editUrl":"https://github.com/rtl-buddy/rtl_buddy/edit/main/docs/known-issues.md","tags":[],"version":"current","frontMatter":{"description":"Current rtl_buddy limitations, surprising behavior, and required workarounds by workflow."},"sidebar":"docs","previous":{"title":"Migrations","permalink":"/rtl_buddy/dev/migrations"},"next":{"title":"Contributing","permalink":"/rtl_buddy/dev/CONTRIBUTING"}}');var r=s(4848),i=s(8453);const o={description:"Current rtl_buddy limitations, surprising behavior, and required workarounds by workflow."},a="Quirks & Known Issues",d={},c=[{value:"XPM CDC macros require rtl-buddy-cdc 0.4 or later",id:"xpm-cdc-macros-require-rtl-buddy-cdc-04-or-later",level:2},{value:"Coverage uses the platform builder",id:"coverage-uses-the-platform-builder",level:2},{value:"Verilator randomized runs may not reproduce",id:"verilator-randomized-runs-may-not-reproduce",level:2},{value:"Tool-path fallback can select another installation",id:"tool-path-fallback-can-select-another-installation",level:2},{value:"Hook scripts are not normal standalone scripts",id:"hook-scripts-are-not-normal-standalone-scripts",level:2},{value:"Compilation-unit bind requires the slang frontend",id:"compilation-unit-bind-requires-the-slang-frontend",level:2},{value:"Verify that <code>anyconst</code> elaborates",id:"verify-that-anyconst-elaborates",level:2},{value:"Narrow VCS access flags suppress cocotb defaults",id:"narrow-vcs-access-flags-suppress-cocotb-defaults",level:2},{value:"VCS license waits pause the simulation timeout",id:"vcs-license-waits-pause-the-simulation-timeout",level:2},{value:"AXI profiling converts VCS VPD traces",id:"axi-profiling-converts-vcs-vpd-traces",level:2},{value:"A timeout kill can leave <code>test.log</code> with an unflushed tail",id:"a-timeout-kill-can-leave-testlog-with-an-unflushed-tail",level:2},{value:"pywellen must remain below 0.25",id:"pywellen-must-remain-below-025",level:2},{value:"Artifact locking is per tree and per host",id:"artifact-locking-is-per-tree-and-per-host",level:2},{value:"Tool flows delete their previous outputs before running",id:"tool-flows-delete-their-previous-outputs-before-running",level:2},{value:"Dispatch changes build behavior",id:"dispatch-changes-build-behavior",level:2},{value:"local-parallel enforces only the job count",id:"local-parallel-enforces-only-the-job-count",level:2},{value:"Oversized resource groups are split into several arrays",id:"oversized-resource-groups-are-split-into-several-arrays",level:2},{value:"Quote dispatch time values",id:"quote-dispatch-time-values",level:2},{value:"Dispatch build jobs cover the whole suite",id:"dispatch-build-jobs-cover-the-whole-suite",level:2},{value:"Slurm retry reuses artifact paths",id:"slurm-retry-reuses-artifact-paths",level:2},{value:"Slurm memory advice depends on accounting samples",id:"slurm-memory-advice-depends-on-accounting-samples",level:2},{value:"<code>rb nvim-install</code> requires git and network access",id:"rb-nvim-install-requires-git-and-network-access",level:2},{value:"Generated <code>run.f</code> files are checkout-specific",id:"generated-runf-files-are-checkout-specific",level:2},{value:"Shared-build dependency tracking varies by simulator",id:"shared-build-dependency-tracking-varies-by-simulator",level:2},{value:"The first run after upgrading recompiles every shared build",id:"the-first-run-after-upgrading-recompiles-every-shared-build",level:2},{value:"Check every <code>toplevel:</code> before upgrading: it now roots the compile",id:"check-every-toplevel-before-upgrading-it-now-roots-the-compile",level:2},{value:"Yosys-backed flows do not support whitespace in paths",id:"yosys-backed-flows-do-not-support-whitespace-in-paths",level:2},{value:"Static-lifetime functions corrupt the netlist under the slang frontend",id:"static-lifetime-functions-corrupt-the-netlist-under-the-slang-frontend",level:2},{value:"Unknown synthesis overrides are ignored after a warning",id:"unknown-synthesis-overrides-are-ignored-after-a-warning",level:2},{value:"FPV COI analysis is best-effort",id:"fpv-coi-analysis-is-best-effort",level:2},{value:"FPGA bitstream generation relaxes two I/O DRCs",id:"fpga-bitstream-generation-relaxes-two-io-drcs",level:2},{value:"FPGA timing is optional unless gated",id:"fpga-timing-is-optional-unless-gated",level:2},{value:"Graph coverage source changes attribution",id:"graph-coverage-source-changes-attribution",level:2},{value:"A cocotb test over an opted-out model keeps a dangling DUT edge",id:"a-cocotb-test-over-an-opted-out-model-keeps-a-dangling-dut-edge",level:2},{value:"The viewer distribution and executable have different names",id:"the-viewer-distribution-and-executable-have-different-names",level:2},{value:"Verible lint findings are on stderr",id:"verible-lint-findings-are-on-stderr",level:2}];function l(e){const t={a:"a",code:"code",em:"em",h1:"h1",h2:"h2",header:"header",p:"p",pre:"pre",strong:"strong",...(0,i.R)(),...e.components};return(0,r.jsxs)(r.Fragment,{children:[(0,r.jsx)(t.header,{children:(0,r.jsx)(t.h1,{id:"quirks--known-issues",children:"Quirks & Known Issues"})}),"\n",(0,r.jsx)(t.p,{children:"Use this page when behavior differs from the normal workflow. Each section states the effect and the action to take; resolved bugs belong in release notes or migrations, not here."}),"\n",(0,r.jsx)(t.h2,{id:"xpm-cdc-macros-require-rtl-buddy-cdc-04-or-later",children:"XPM CDC macros require rtl-buddy-cdc 0.4 or later"}),"\n",(0,r.jsxs)(t.p,{children:["rtl-buddy-cdc 0.3.x treats ",(0,r.jsx)(t.code,{children:"xpm_cdc_*"})," instances as dual-clock blackboxes, reports ",(0,r.jsx)(t.code,{children:"CDC-BBX"}),", and drops their crossings from the report and domain map. Upgrade with ",(0,r.jsx)(t.code,{children:"uv tool install -U rtl-buddy-cdc"}),". Waivers can hide the 0.3.x finding but cannot recover crossings beyond the macro."]}),"\n",(0,r.jsx)(t.h2,{id:"coverage-uses-the-platform-builder",children:"Coverage uses the platform builder"}),"\n",(0,r.jsxs)(t.p,{children:["Coverage collection and labels use the platform-selected builder, even when a suite or test selects another ",(0,r.jsx)(t.code,{children:"builder:"}),". A mismatch can mislabel or misparse coverage. Use ",(0,r.jsx)(t.code,{children:"--builder <name>"})," for the run or make that builder the platform default. See ",(0,r.jsx)(t.a,{href:"/rtl_buddy/dev/reference/yaml",children:"YAML Formats"}),"."]}),"\n",(0,r.jsx)(t.h2,{id:"verilator-randomized-runs-may-not-reproduce",children:"Verilator randomized runs may not reproduce"}),"\n",(0,r.jsxs)(t.p,{children:["Verilator can produce different behavior for the same random seed. Use VCS with ",(0,r.jsx)(t.code,{children:"-xlrm hier_inst_seed"})," when reproducibility is required, and give instances stable explicit names."]}),"\n",(0,r.jsxs)(t.p,{children:["With hierarchical seeding, VCS writes ",(0,r.jsx)(t.code,{children:"HierInstanceSeed.txt"})," in the simulation directory. If it is missing, rtl_buddy logs ",(0,r.jsx)(t.code,{children:"sim.hier_seed_missing"})," and cannot record the seed, but does not change the test verdict."]}),"\n",(0,r.jsx)(t.h2,{id:"tool-path-fallback-can-select-another-installation",children:"Tool-path fallback can select another installation"}),"\n",(0,r.jsxs)(t.p,{children:["Configured tool directories take precedence only when they contain the requested executable. Otherwise rtl_buddy may use a matching executable on ",(0,r.jsx)(t.code,{children:"PATH"})," and logs a fallback warning."]}),"\n",(0,r.jsxs)(t.p,{children:["Warnings for fallback paths and unresolved variables are emitted once per process. Restart long-running ",(0,r.jsx)(t.code,{children:"rb hub"})," or ",(0,r.jsx)(t.code,{children:"rb mcp"})," processes after changing ",(0,r.jsx)(t.code,{children:"root_config.yaml"}),", ",(0,r.jsx)(t.code,{children:".rtl-buddy/.env"}),", or the environment if you need the warning to be evaluated again."]}),"\n",(0,r.jsx)(t.h2,{id:"hook-scripts-are-not-normal-standalone-scripts",children:"Hook scripts are not normal standalone scripts"}),"\n",(0,r.jsxs)(t.p,{children:[(0,r.jsx)(t.code,{children:"sweep"})," and ",(0,r.jsx)(t.code,{children:"preproc"})," execute with the invocation directory as CWD and with ",(0,r.jsx)(t.code,{children:'__name__ == "__rtl_buddy_hook__"'}),". Use injected ",(0,r.jsx)(t.code,{children:"suite_dir"}),", ",(0,r.jsx)(t.code,{children:"artifact_dir"}),", and ",(0,r.jsx)(t.code,{children:"run_artifact_dir"})," paths; do not put required hook logic behind ",(0,r.jsx)(t.code,{children:'if __name__ == "__main__":'}),"."]}),"\n",(0,r.jsxs)(t.p,{children:["rtl_buddy captures Python-level ",(0,r.jsx)(t.code,{children:"print()"})," output as ",(0,r.jsx)(t.code,{children:"hook.stdout"})," events. The capture has no ",(0,r.jsx)(t.code,{children:".buffer"})," or file descriptor, and child-process output bypasses it. Capture child output explicitly and print the text you want logged. For a generator that can write only relative to CWD, temporarily change to ",(0,r.jsx)(t.code,{children:"suite_dir"})," and restore the previous directory. See ",(0,r.jsx)(t.a,{href:"/rtl_buddy/dev/concepts/plugins",children:"Plugins"}),"."]}),"\n",(0,r.jsx)(t.h2,{id:"compilation-unit-bind-requires-the-slang-frontend",children:"Compilation-unit bind requires the slang frontend"}),"\n",(0,r.jsxs)(t.p,{children:["Yosys's native ",(0,r.jsx)(t.code,{children:"verilog"})," frontend does not resolve a top-level ",(0,r.jsx)(t.code,{children:"bind"}),", so no formal cells elaborate. rtl_buddy fails a property-based proof that would otherwise pass vacuously. Set ",(0,r.jsx)(t.code,{children:"frontend: slang"})," and configure the yosys-slang plugin. Inline assertions do not need this guard. See ",(0,r.jsx)(t.a,{href:"/rtl_buddy/dev/concepts/fpv",children:"Formal Property Verification"}),"."]}),"\n",(0,r.jsxs)(t.h2,{id:"verify-that-anyconst-elaborates",children:["Verify that ",(0,r.jsx)(t.code,{children:"anyconst"})," elaborates"]}),"\n",(0,r.jsxs)(t.p,{children:["Some yosys-slang builds drop ",(0,r.jsx)(t.code,{children:"(* anyconst *)"})," without producing a ",(0,r.jsx)(t.code,{children:"$anyconst"})," cell. The signal then varies freely each cycle and can invalidate symbolic-index proofs. Check the elaborated design before relying on it:"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-bash",children:"yosys -p 'read_slang ...; prep -top dut; select -assert-min 1 t:$anyconst'\n"})}),"\n",(0,r.jsx)(t.p,{children:"Use a behavioral reference model when portable data-integrity checking matters."}),"\n",(0,r.jsx)(t.h2,{id:"narrow-vcs-access-flags-suppress-cocotb-defaults",children:"Narrow VCS access flags suppress cocotb defaults"}),"\n",(0,r.jsxs)(t.p,{children:["For cocotb, rtl_buddy adds VPI access unless any configured compile option already starts with ",(0,r.jsx)(t.code,{children:"-debug_access"})," or ",(0,r.jsx)(t.code,{children:"+acc"}),". A narrower configured flag therefore suppresses the full default and may prevent signal writes. Remove the narrow flag or configure sufficient access, such as ",(0,r.jsx)(t.code,{children:"-debug_access+all"})," and ",(0,r.jsx)(t.code,{children:"+acc+rw"}),"."]}),"\n",(0,r.jsx)(t.h2,{id:"vcs-license-waits-pause-the-simulation-timeout",children:"VCS license waits pause the simulation timeout"}),"\n",(0,r.jsxs)(t.p,{children:["When VCS prints its license-queue banner, rtl_buddy pauses ",(0,r.jsx)(t.code,{children:"sim_timeout"})," until simulator output resumes, for up to one hour. A queued run can therefore outlive its nominal timeout. If a newer VCS banner is not recognized, the clock may resume too early; a timeout beside license messages in ",(0,r.jsx)(t.code,{children:"test.err"})," indicates this case. Use the builder's ",(0,r.jsx)(t.code,{children:"extra-sim-timeout"})," as a backstop."]}),"\n",(0,r.jsx)(t.h2,{id:"axi-profiling-converts-vcs-vpd-traces",children:"AXI profiling converts VCS VPD traces"}),"\n",(0,r.jsxs)(t.p,{children:[(0,r.jsx)(t.code,{children:"rb axi-profile run"})," converts ",(0,r.jsx)(t.code,{children:"vcdplus.vpd"})," with ",(0,r.jsx)(t.code,{children:"vpd2vcd"}),", trying ",(0,r.jsx)(t.code,{children:"-full64"})," first and the legacy form second. Conversion details go to ",(0,r.jsx)(t.code,{children:"artefacts/axi/<test>/vpd-convert.log"}),"."]}),"\n",(0,r.jsxs)(t.p,{children:["If ",(0,r.jsx)(t.code,{children:"vcd2fst"})," is installed, the VCD becomes a cached ",(0,r.jsx)(t.code,{children:"vcdplus.fst"}),"; otherwise rtl_buddy keeps and ingests the larger VCD with a warning. Cached files live beside the original VPD in the test artifact directory."]}),"\n",(0,r.jsxs)(t.h2,{id:"a-timeout-kill-can-leave-testlog-with-an-unflushed-tail",children:["A timeout kill can leave ",(0,r.jsx)(t.code,{children:"test.log"})," with an unflushed tail"]}),"\n",(0,r.jsxs)(t.p,{children:["When ",(0,r.jsx)(t.code,{children:"sim_timeout"})," expires, the simulator may be terminated before flushing output. ",(0,r.jsx)(t.code,{children:"test.log"})," can end mid-line or at a power-of-two byte count, so its final bytes are not an exact stop location. Follow the ",(0,r.jsx)(t.a,{href:"/rtl_buddy/dev/concepts/tests#triaging-sim-hit-timeout",children:"timeout triage order"})," before raising the limit."]}),"\n",(0,r.jsx)(t.h2,{id:"pywellen-must-remain-below-025",children:"pywellen must remain below 0.25"}),"\n",(0,r.jsxs)(t.p,{children:[(0,r.jsx)(t.code,{children:"rb wave"})," annotations and ",(0,r.jsx)(t.code,{children:"rb saif"})," require pywellen's removed random-access API, so the supported range is ",(0,r.jsx)(t.code,{children:">=0.20,<0.25"}),". A forced newer version fails at launch with ",(0,r.jsx)(t.code,{children:"pywellen.api_missing"}),"; restore the supported dependency range."]}),"\n",(0,r.jsx)(t.h2,{id:"artifact-locking-is-per-tree-and-per-host",children:"Artifact locking is per tree and per host"}),"\n",(0,r.jsxs)(t.p,{children:["Artifact-writing commands take ",(0,r.jsx)(t.code,{children:"<artifact_root>/.rtl-buddy.lock"})," and fail immediately on same-host contention. The file remains after release; kernel lock state, not file presence, determines whether the tree is locked."]}),"\n",(0,r.jsx)(t.p,{children:"The lock is intentionally coarse across command families and is not assumed to coordinate different NFS hosts. Dispatched worker jobs skip it because they write planned subdirectories, so do not start another command against a tree with a dispatch run in flight."}),"\n",(0,r.jsx)(t.h2,{id:"tool-flows-delete-their-previous-outputs-before-running",children:"Tool flows delete their previous outputs before running"}),"\n",(0,r.jsxs)(t.p,{children:[(0,r.jsx)(t.code,{children:"rb cdc"}),", ",(0,r.jsx)(t.code,{children:"rb synth"}),", ",(0,r.jsx)(t.code,{children:"rb fpga"}),", ",(0,r.jsx)(t.code,{children:"rb pnr"}),", and ",(0,r.jsx)(t.code,{children:"rb power"})," remove the outputs they are about to write \u2014 reports, domain maps, synthesis netlists, DEF/ODB, GDS/PNG, bitstream \u2014 from the run's artifact directory before invoking the tool. ",(0,r.jsx)(t.code,{children:"rb hub"})," does the same for the ",(0,r.jsx)(t.code,{children:"view.json"})," and domain map it caches under ",(0,r.jsx)(t.code,{children:".rtl-buddy/cache/"}),', which outlive the build that filled them. An exit code cannot distinguish "produced nothing to report" from "crashed before writing" (rtl-buddy-cdc\'s exit 1 means rule violations were found), so a report left by an earlier run would otherwise be parsed and its counts reported as the current result. Clearing first makes an absent report absent, and the flow then says so and names its log.']}),"\n",(0,r.jsxs)(t.p,{children:["A run that cannot find its backend tool is the exception: it deletes nothing, because a machine without the tool never ran it and has no business removing what a machine that has it produced. That covers ",(0,r.jsx)(t.code,{children:"rb cdc"})," (both backends \u2014 the open analyzer is not an rtl_buddy dependency, so a project can legitimately run without it), ",(0,r.jsx)(t.code,{children:"rb fpga"})," and ",(0,r.jsx)(t.code,{children:"rb power"}),". ",(0,r.jsx)(t.code,{children:"rb synth"})," and ",(0,r.jsx)(t.code,{children:"rb pnr"})," clear regardless, tool present or not, because their netlists and their DEF/ODB are resolved by path by a ",(0,r.jsx)(t.em,{children:"later"})," command \u2014 a missing tool still means there is no fresh netlist, and ",(0,r.jsx)(t.code,{children:"rb pnr"})," or ",(0,r.jsx)(t.code,{children:"rb power"})," must not silently use the old one. A configuration error is never a skip: an unknown ",(0,r.jsx)(t.code,{children:"platform:"}),", or a part a backend cannot build, is reported whether or not the tool is present, and clears the outputs on its way out."]}),"\n",(0,r.jsxs)(t.p,{children:["The clearing happens early, not just before the tool starts: a rerun that fails on a filelist error, an unresolvable config, or any other check still leaves nothing behind. Outputs a later command consumes \u2014 the synthesis netlists ",(0,r.jsx)(t.code,{children:"rb pnr"})," and ",(0,r.jsx)(t.code,{children:"rb power"})," resolve, and pnr's DEF and ODB \u2014 are cleared as the very first thing the run does, ahead of even the tool-availability check, because a missing tool still means there is no fresh netlist for the downstream command to use. Outputs only read back within the same run are cleared just after that check instead."]}),"\n",(0,r.jsxs)(t.p,{children:["Outputs named after the design's top \u2014 ",(0,r.jsx)(t.code,{children:"<top>.bit"}),", ",(0,r.jsx)(t.code,{children:"<design>.routed.odb"})," \u2014 are matched by suffix rather than by name, so editing a run's ",(0,r.jsx)(t.code,{children:"model:"})," or ",(0,r.jsx)(t.code,{children:"top:"})," does not strand the previous top's files in the same directory. Another command's durable outputs are never matched this way either: a run's ",(0,r.jsx)(t.code,{children:"result.json"}),", its build cache ",(0,r.jsx)(t.code,{children:"rb-compile-stamp.json"}),", the dispatch envelopes, and the sibling commands' reports \u2014 ",(0,r.jsx)(t.code,{children:"cdc.json"}),", the CDC domain maps, ",(0,r.jsx)(t.code,{children:"power.rpt"}),", the synthesis netlists \u2014 all survive. This matters because an artifact directory is keyed on a run's ",(0,r.jsx)(t.em,{children:"name"})," and names need not be unique across commands: an ",(0,r.jsx)(t.code,{children:"rb fpga"})," run, a CDC analysis and a simulation test called the same thing all share one directory."]}),"\n",(0,r.jsxs)(t.p,{children:["Sharing a name is still worth avoiding, because clearing is not the only way two commands collide. An FPGA run and a power run must not share a name within one suite: both own ",(0,r.jsx)(t.code,{children:"artefacts/<name>/power.rpt"})," and the second to run overwrites the first. Ownership cannot be told apart by filename, so rtl_buddy does not try \u2014 give them distinct names. An artifact directory belongs to one run, so everything carrying a suffix that flow writes is that flow's own output; nothing else in the directory is touched, and the scan never recurses into a workdir a tool owns."]}),"\n",(0,r.jsxs)(t.p,{children:["A tool that writes an output and ",(0,r.jsx)(t.em,{children:"then"})," fails counts as a failure like any other, and every flow removes what it wrote on the way out. Yosys writes its netlist before the trailing ",(0,r.jsx)(t.code,{children:"stat"})," that can error; the CDC analyzers write their report before they finish; Vivado's FPGA flow writes all five reports before it reaches ",(0,r.jsx)(t.code,{children:"write_bitstream"}),"; OpenROAD writes ",(0,r.jsx)(t.code,{children:"power.rpt"})," and the routed database before its script ends; KLayout can leave a zero-length GDS or a half-rendered PNG; and the hierarchy renderer can write a ",(0,r.jsx)(t.code,{children:"view.json"})," before exiting non-zero or emitting a schema this rtl_buddy rejects. Those outputs are removed again, so a run that reports a failure has published nothing. A configuration error \u2014 an unknown ",(0,r.jsx)(t.code,{children:"platform:"}),", a part a backend cannot build \u2014 clears them too; it is a failed run, not a skip."]}),"\n",(0,r.jsxs)(t.p,{children:["Consequently a failed rerun leaves no output at all rather than the previous run's. This propagates: a failed ",(0,r.jsx)(t.code,{children:"rb synth"})," leaves no netlist, so ",(0,r.jsx)(t.code,{children:"rb pnr"})," and ",(0,r.jsx)(t.code,{children:"rb power"})," report that you need to run ",(0,r.jsx)(t.code,{children:"rb synth"})," first instead of consuming the previous netlist. An ",(0,r.jsx)(t.code,{children:"rb fpga"})," run without ",(0,r.jsx)(t.code,{children:"--bitstream"})," likewise removes a previously built ",(0,r.jsx)(t.code,{children:"<top>.bit"}),". Copy an artifact you want to compare against out of ",(0,r.jsx)(t.code,{children:"artefacts/<name>/"})," before rerunning. Logs are exempt: each flow truncates its own log, so a crashed run still has one to read."]}),"\n",(0,r.jsx)(t.p,{children:"An artifact that cannot be deleted \u2014 a permissions problem, or a directory where a file belongs \u2014 fails the run with a fatal error rather than letting it proceed, since continuing would risk reporting the previous run's numbers."}),"\n",(0,r.jsx)(t.h2,{id:"dispatch-changes-build-behavior",children:"Dispatch changes build behavior"}),"\n",(0,r.jsxs)(t.p,{children:[(0,r.jsx)(t.code,{children:"--dispatch"})," implies ",(0,r.jsx)(t.code,{children:"--share-build"})," and rejects ",(0,r.jsx)(t.code,{children:"--early-stop"}),". ",(0,r.jsx)(t.code,{children:"cfg-dispatch.backend"})," defaults ",(0,r.jsx)(t.code,{children:"regression"})," and ",(0,r.jsx)(t.code,{children:"randtest"}),", but ",(0,r.jsx)(t.code,{children:"rb test"})," remains local unless ",(0,r.jsx)(t.code,{children:"--dispatch"})," is explicit. A one-seed ",(0,r.jsx)(t.code,{children:"randtest"})," replay also stays local."]}),"\n",(0,r.jsxs)(t.p,{children:["Shareable builders compile once per compile key. Builders that cannot share compile in their jobs; fanned-out tests still use a build job to serialize access to their compile directory. See ",(0,r.jsx)(t.a,{href:"/rtl_buddy/dev/concepts/dispatch",children:"Parallel Dispatch"}),"."]}),"\n",(0,r.jsx)(t.h2,{id:"local-parallel-enforces-only-the-job-count",children:"local-parallel enforces only the job count"}),"\n",(0,r.jsxs)(t.p,{children:["The ",(0,r.jsx)(t.code,{children:"local-parallel"})," backend ignores CPU, memory, time, array-throttle, array-size, and right-sizing settings. ",(0,r.jsx)(t.code,{children:"max-jobs-per-array"})," and ",(0,r.jsx)(t.code,{children:"max-array-size"})," describe Slurm job arrays, of which this backend submits none, so neither throttles nor splits anything here. ",(0,r.jsx)(t.code,{children:"-j"})," or ",(0,r.jsx)(t.code,{children:"cfg-dispatch.jobs"})," is the only limit, so size concurrency for the heaviest test's memory use."]}),"\n",(0,r.jsxs)(t.p,{children:[(0,r.jsx)(t.code,{children:"cfg-dispatch.compile.parallel"})," is the exception: it is not a reservation but concurrency the build job itself honours, and that job occupies one pool slot while fanning out inside it. The real ceiling on the host is therefore ",(0,r.jsx)(t.code,{children:"jobs"})," multiplied by ",(0,r.jsx)(t.code,{children:"compile.parallel"}),", and nothing clamps it. Size the two together."]}),"\n",(0,r.jsxs)(t.p,{children:["Normal interruption terminates the worker process groups. ",(0,r.jsx)(t.code,{children:"SIGKILL"})," of the head process cannot run cleanup and can orphan ",(0,r.jsx)(t.code,{children:"rb _test-job"})," children; inspect and stop them after a hard CI timeout or ",(0,r.jsx)(t.code,{children:"kill -9"}),"."]}),"\n",(0,r.jsx)(t.h2,{id:"oversized-resource-groups-are-split-into-several-arrays",children:"Oversized resource groups are split into several arrays"}),"\n",(0,r.jsxs)(t.p,{children:["A resource group larger than the cluster's Slurm ",(0,r.jsx)(t.code,{children:"MaxArraySize"})," cannot be one job array. rtl_buddy submits it as several, each holding at most ",(0,r.jsx)(t.code,{children:"MaxArraySize - 1"})," elements \u2014 or fewer where the cluster sets ",(0,r.jsx)(t.code,{children:"SchedulerParameters=max_array_tasks"}),", an inclusive cap on the tasks in one array that ",(0,r.jsx)(t.code,{children:"scontrol"})," reports separately and that the slice size takes the minimum with. Two consequences are worth planning for: ",(0,r.jsx)(t.code,{children:"max-jobs-per-array"})," throttles each slice, so the group's peak concurrency is that throttle multiplied by the number of slices; and a slice's manifest and element logs live under ",(0,r.jsx)(t.code,{children:"slice-N/"})," in the run's dispatch directory instead of directly in it. A group that fits in a single array keeps the flat layout."]}),"\n",(0,r.jsxs)(t.p,{children:["The limit is read from ",(0,r.jsx)(t.code,{children:"scontrol show config"})," once per cluster per run, before the run's first array submit, with the ",(0,r.jsx)(t.code,{children:"-M"})," of any cross-cluster ",(0,r.jsx)(t.code,{children:"sbatch-args"})," so the answer describes the cluster the arrays are submitted to. ",(0,r.jsx)(t.code,{children:"SBATCH_CLUSTERS"})," selects a cluster the same way, with ",(0,r.jsx)(t.code,{children:"sbatch-args"})," winning. A selection naming several clusters \u2014 ",(0,r.jsx)(t.code,{children:"--clusters=a,b"}),", or the reserved ",(0,r.jsx)(t.code,{children:"all"})," \u2014 leaves the limit unknown, since Slurm chooses between them at submit; pin ",(0,r.jsx)(t.code,{children:"cfg-dispatch.max-array-size"})," for those. Where the submit host cannot run ",(0,r.jsx)(t.code,{children:"scontrol"}),", nothing is split and sbatch refuses an oversized group with ",(0,r.jsx)(t.code,{children:"Invalid job array specification"}),"; set ",(0,r.jsx)(t.code,{children:"cfg-dispatch.max-array-size"})," to the cluster's value, and ",(0,r.jsx)(t.code,{children:"cfg-dispatch.max-array-tasks"})," too where the cluster caps tasks-per-array below it \u2014 each ceiling is configured, and layered over the probe, on its own, and either one alone is enough to split a group. ",(0,r.jsx)(t.code,{children:"dispatch.max_array_size_unknown"})," in the run log names that case, and the submit failure itself repeats the hint. A resolved limit is recorded at debug level as ",(0,r.jsx)(t.code,{children:"dispatch.max_array_size"}),", whose ",(0,r.jsx)(t.code,{children:"source"})," field reads ",(0,r.jsx)(t.code,{children:"config"})," or ",(0,r.jsx)(t.code,{children:"scontrol"}),"."]}),"\n",(0,r.jsx)(t.h2,{id:"quote-dispatch-time-values",children:"Quote dispatch time values"}),"\n",(0,r.jsxs)(t.p,{children:["YAML 1.1 parses an unquoted value such as ",(0,r.jsx)(t.code,{children:"time: 4:00:00"})," as an integer. rtl_buddy rejects it rather than submit a 10-day Slurm reservation. Use ",(0,r.jsx)(t.code,{children:'time: "4:00:00"'})," or a quoted minute count everywhere ",(0,r.jsx)(t.code,{children:"resources:"})," appears."]}),"\n",(0,r.jsx)(t.h2,{id:"dispatch-build-jobs-cover-the-whole-suite",children:"Dispatch build jobs cover the whole suite"}),"\n",(0,r.jsxs)(t.p,{children:["One suite build job compiles every unique compile key, and all simulation jobs wait for the complete build. ",(0,r.jsx)(t.code,{children:"cfg-dispatch.compile.parallel"})," compiles that many distinct builds at once inside that job, so ",(0,r.jsx)(t.code,{children:"compile.time"})," must cover the longest batch rather than the serial total, and ",(0,r.jsx)(t.code,{children:"compile.mem"})," must cover that many concurrent elaborations because only ",(0,r.jsx)(t.code,{children:"cpus"})," is scaled for you. Count ",(0,r.jsx)(t.code,{children:"compile.start"})," events to estimate the work."]}),"\n",(0,r.jsxs)(t.p,{children:["The build job's reservation is ",(0,r.jsx)(t.code,{children:"compile.cpus"})," multiplied by ",(0,r.jsx)(t.code,{children:"min(parallel, planned tests)"}),". The cap is planned tests, not distinct compile keys: the head cannot know the keys without writing filelists on the submit host, which is the build job's own work. A suite whose tests share compile keys therefore reserves CPUs for build slots that never run \u2014 twenty tests over three keys with ",(0,r.jsx)(t.code,{children:"parallel: 8"})," reserves eight builds' worth of CPUs for three. Set ",(0,r.jsx)(t.code,{children:"parallel"})," to the expected distinct-build count, not to the test count, and confirm it against the ",(0,r.jsx)(t.code,{children:"(build job)"})," row of the reservation advice."]}),"\n",(0,r.jsxs)(t.p,{children:["Under dispatch, ",(0,r.jsx)(t.code,{children:"sweep"})," runs once on the head, while ",(0,r.jsx)(t.code,{children:"preproc"})," runs in the build job and again in every simulation job. Make ",(0,r.jsx)(t.code,{children:"preproc"})," idempotent. Write shared generated files atomically to ",(0,r.jsx)(t.code,{children:"artifact_dir"}),"; write run-dependent files to ",(0,r.jsx)(t.code,{children:"run_artifact_dir"}),"."]}),"\n",(0,r.jsxs)(t.p,{children:[(0,r.jsx)(t.code,{children:"cfg-dispatch.compile.parallel"})," above 1 adds a second requirement: no config's ",(0,r.jsx)(t.code,{children:"preproc"})," may mutate an input another config compiles. The build job runs every hook before any builder starts, because a config's compile key is only knowable after its own hook ran, so a hook that regenerates a suite-level file overwrites it for configs that have already been fingerprinted. At the default ",(0,r.jsx)(t.code,{children:"parallel: 1"})," the job still runs ",(0,r.jsx)(t.code,{children:"preproc"})," and compile per config in turn, so a generator hook that owns one shared file per config is safe there. Simulation jobs make the same demand at any setting, each re-running ",(0,r.jsx)(t.code,{children:"preproc"})," on its own node."]}),"\n",(0,r.jsxs)(t.p,{children:["Simulation jobs rely on the build stamp to skip recompilation. The stamp records a content hash of every tracked input under the project root, so content decides: a preprocessor that regenerates a filelist source byte-for-byte no longer invalidates anything, while one that changes a source's content invalidates every stamp and can trigger concurrent compiles into one directory. ",(0,r.jsx)(t.code,{children:"compile.prebuilt_stamp_invalid"})," identifies this case. Avoid changing shared inputs from a per-test preprocessor."]}),"\n",(0,r.jsxs)(t.p,{children:["A design compile error is reported as ",(0,r.jsx)(t.code,{children:"CompileFail"}),", not ",(0,r.jsx)(t.code,{children:"DispatchFail"}),". Infrastructure failures remain ",(0,r.jsx)(t.code,{children:"DispatchFail"}),". A build the build job recorded as failed with a builder exit code, on the same inputs the simulation job would compile, is not recompiled by its simulation jobs \u2014 the summary row carries the build job's error and logs; fix the design or the build reservation, not the simulation one. A simulation job recompiles only when the stamp is invalid without that evidence (stamp drift, a build-side setup failure, or inputs that changed since the failed build), at simulation size with its transcript in the run's own ",(0,r.jsx)(t.code,{children:"compile.retry.log"})," (under ",(0,r.jsx)(t.code,{children:"run-NNNN/"})," for a fanned-out test), so size the simulation reservation for compilation only when relying on that recovery path."]}),"\n",(0,r.jsx)(t.h2,{id:"slurm-retry-reuses-artifact-paths",children:"Slurm retry reuses artifact paths"}),"\n",(0,r.jsxs)(t.p,{children:["A retry overwrites the first attempt's simulation capture and per-job rtl_buddy log; only ",(0,r.jsx)(t.code,{children:"slurm-<tag>-retry<N>.log"})," remains attempt-specific. Use scheduler logs and the head's ",(0,r.jsx)(t.code,{children:"dispatch.result_missing"})," event when diagnosing retries."]}),"\n",(0,r.jsxs)(t.p,{children:[(0,r.jsx)(t.code,{children:"max-wait"})," applies to each attempt, not the whole run, and includes the requested backoff. A later ",(0,r.jsx)(t.code,{children:"--begin"})," in ",(0,r.jsx)(t.code,{children:"sbatch-args"})," overrides rtl_buddy's retry delay because Slurm uses the last duplicate option. Remove custom ",(0,r.jsx)(t.code,{children:"--begin"})," when using retry backoff."]}),"\n",(0,r.jsx)(t.h2,{id:"slurm-memory-advice-depends-on-accounting-samples",children:"Slurm memory advice depends on accounting samples"}),"\n",(0,r.jsxs)(t.p,{children:["rtl_buddy requests one-second task accounting unless ",(0,r.jsx)(t.code,{children:"sbatch-args"})," already sets ",(0,r.jsx)(t.code,{children:"--acctg-freq"}),". Memory advice is suppressed when the longest run ends within the active sampling interval because ",(0,r.jsx)(t.code,{children:"MaxRSS"})," is unreliable; time and CPU advice remain available."]}),"\n",(0,r.jsx)(t.p,{children:"Right-sizing suggestions also have fixed five-minute and 128 MB floors and require at least 25% savings. Very small reservations can therefore produce no reduction advice even when utilization is low."}),"\n",(0,r.jsxs)(t.h2,{id:"rb-nvim-install-requires-git-and-network-access",children:[(0,r.jsx)(t.code,{children:"rb nvim-install"})," requires git and network access"]}),"\n",(0,r.jsxs)(t.p,{children:["The default install clones a pinned ",(0,r.jsx)(t.code,{children:"rtl-buddy-nvim"})," revision. For an air-gapped system, provide a local checkout:"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-bash",children:"rb nvim-install --source /path/to/rtl-buddy-nvim --ref <ref>\n"})}),"\n",(0,r.jsx)(t.p,{children:"The plugin pin must speak the hub protocol shipped by rtl_buddy; maintainers update both together."}),"\n",(0,r.jsxs)(t.h2,{id:"generated-runf-files-are-checkout-specific",children:["Generated ",(0,r.jsx)(t.code,{children:"run.f"})," files are checkout-specific"]}),"\n",(0,r.jsxs)(t.p,{children:["rtl_buddy writes explicit source entries as absolute paths so Verilator cannot resolve a relative source through an include or library directory in another checkout. ",(0,r.jsx)(t.code,{children:"+incdir+"})," and ",(0,r.jsx)(t.code,{children:"-y"})," search directories are written absolute for the same reason. A relative search directory is resolved by the builder rather than by rtl_buddy: ",(0,r.jsx)(t.code,{children:"-f"})," entries are read relative to the builder's working directory, and even a builder started in ",(0,r.jsx)(t.code,{children:"run.f"}),"'s own directory disagrees whenever a symlink sits between that directory and the design, because a relative path collapses ",(0,r.jsx)(t.code,{children:".."})," textually while a process walks it physically. Do not commit or copy ",(0,r.jsx)(t.code,{children:"run.f"})," between checkouts. Use one symlink spelling of a checkout consistently, because path spelling affects compile keys."]}),"\n",(0,r.jsxs)(t.p,{children:["Two path spellings cannot be pinned. An include directory whose path contains ",(0,r.jsx)(t.code,{children:"+"})," keeps its relative spelling, because filelist parsers read ",(0,r.jsx)(t.code,{children:"+incdir+a+b"})," as two directories and quoting does not change that; ",(0,r.jsx)(t.code,{children:"filelist.incdir_unrepresentable"})," names those entries, and they stay dependent on the builder's working directory until the ",(0,r.jsx)(t.code,{children:"+"})," is out of the path. A path containing whitespace is quoted instead, which Verilator's ",(0,r.jsx)(t.code,{children:"-f"})," parser understands; Icarus's does not, and VCS is unverified, so keep whitespace out of checkout paths for those simulators."]}),"\n",(0,r.jsx)(t.p,{children:"On a cluster with different mount paths per node, a stamp from one node may not validate on another. This causes a safe recompile, not compilation of the wrong source."}),"\n",(0,r.jsxs)(t.p,{children:["Because the compile key hashes the text of each ",(0,r.jsx)(t.code,{children:"run.f"})," entry, the first run after upgrading to a release that changed how an entry is spelled recompiles each shared build once. An absolute spelling also makes the key independent of where in the tree the consuming suite sits, so two suites at different depths that share a model now share one build where a relative spelling gave them different keys."]}),"\n",(0,r.jsx)(t.h2,{id:"shared-build-dependency-tracking-varies-by-simulator",children:"Shared-build dependency tracking varies by simulator"}),"\n",(0,r.jsxs)(t.p,{children:["Verilator reports consumed headers, library files, its standard includes, and its binary, so changes invalidate the shared-build stamp. Tracked inputs under the project root are compared by content hash, including a filelist source that lives there as a symlink into a tree outside it. Inputs the root does not cover are compared by size and mtime instead of being hashed on every validation: the toolchain's own includes and binary, any single input above 64 MB (",(0,r.jsx)(t.code,{children:"compile.hash_skipped_large"})," names those), and a reported dependency that resolves outside the root, which is how a header symlinked in from a shared tree is recorded."]}),"\n",(0,r.jsxs)(t.p,{children:["VCS and Icarus emit no dependency file, and their stamps still record ",(0,r.jsx)(t.code,{children:"deps: null"}),". Include directories are covered for them \u2014 and for Verilator \u2014 by the stamp's own listing of every ",(0,r.jsx)(t.code,{children:"+incdir+"})," and ",(0,r.jsx)(t.code,{children:"-y"})," directory the filelist names: one entry per regular file, compared the same content-first way. Editing, adding, or removing a file in one of those directories therefore rebuilds on every builder. The listing is also the only thing that notices a file ",(0,r.jsx)(t.em,{children:"appearing"})," in a ",(0,r.jsx)(t.code,{children:"-y"})," library directory, which no dependency file can report: ",(0,r.jsx)(t.code,{children:"-y"})," resolves by module name on demand, so a file that changes the next elaboration was opened by nobody during the last one. It over-approximates on purpose \u2014 editing a header nothing includes rebuilds a build that did not strictly need it \u2014 because over-invalidating costs a recompile while under-invalidating reports a stale binary as green."]}),"\n",(0,r.jsxs)(t.p,{children:["An ",(0,r.jsx)(t.code,{children:"+incdir+"})," is walked ",(0,r.jsx)(t.strong,{children:"recursively"}),", because ",(0,r.jsx)(t.code,{children:'`include "nested/deep.svh"'})," resolves beneath the directory; a ",(0,r.jsx)(t.code,{children:"-y"})," directory is listed ",(0,r.jsx)(t.strong,{children:"flat"}),", because library resolution maps a module name to a file in the directory itself. Neither is filtered by suffix: ",(0,r.jsx)(t.code,{children:"+libext+"})," can be set in ",(0,r.jsx)(t.code,{children:"builder-opts.compile-time"})," and never reach ",(0,r.jsx)(t.code,{children:"run.f"}),", so a filter derived from ",(0,r.jsx)(t.code,{children:"run.f"})," would silently miss the library file that appears with any other suffix. The cost of the walk is a fraction of a second for a few thousand files, but it is paid on every stamp validation, so pointing an ",(0,r.jsx)(t.code,{children:"+incdir+"})," at a large tree makes every reuse check walk it."]}),"\n",(0,r.jsxs)(t.p,{children:["Two kinds of name are left out of the walk. ",(0,r.jsx)(t.strong,{children:"Directories"}),": dot-directories (",(0,r.jsx)(t.code,{children:".git"}),", ",(0,r.jsx)(t.code,{children:".svn"}),") and RTL Buddy's own managed trees \u2014 the suite's ",(0,r.jsx)(t.code,{children:"artefacts/"}),", ",(0,r.jsx)(t.code,{children:".shared-builds/"}),", and any ",(0,r.jsx)(t.code,{children:"obj_dir*"}),". A project directory genuinely named ",(0,r.jsx)(t.code,{children:"artefacts"})," or ",(0,r.jsx)(t.code,{children:"obj_dir*"})," under an include path is therefore not tracked. ",(0,r.jsx)(t.strong,{children:"Files"}),": a denylist of editor and VCS bookkeeping (",(0,r.jsx)(t.code,{children:".DS_Store"}),", ",(0,r.jsx)(t.code,{children:".gitignore"}),", ",(0,r.jsx)(t.code,{children:".gitattributes"}),", ",(0,r.jsx)(t.code,{children:".gitkeep"}),", ",(0,r.jsx)(t.code,{children:"*.swp"}),", ",(0,r.jsx)(t.code,{children:"*.swo"}),", ",(0,r.jsx)(t.code,{children:"*~"}),", ",(0,r.jsx)(t.code,{children:".#*"}),", ",(0,r.jsx)(t.code,{children:"#*#"}),") plus RTL Buddy's own per-test outputs (",(0,r.jsx)(t.code,{children:"run.f"}),", ",(0,r.jsx)(t.code,{children:"compile.log"}),", ",(0,r.jsx)(t.code,{children:"compile.retry.log"}),", ",(0,r.jsx)(t.code,{children:"test.log"}),", ",(0,r.jsx)(t.code,{children:"test.err"}),", ",(0,r.jsx)(t.code,{children:"test.randseed"}),", ",(0,r.jsx)(t.code,{children:"coverage.dat"}),", ",(0,r.jsx)(t.code,{children:"simv"}),", ",(0,r.jsx)(t.code,{children:"simv.vvp"}),", ",(0,r.jsx)(t.code,{children:"rb-compile-stamp.json"}),", ",(0,r.jsx)(t.code,{children:"result.json"}),", and a dispatched job's ",(0,r.jsx)(t.code,{children:"result-*.json"})," / ",(0,r.jsx)(t.code,{children:"rtl_buddy-*.log"}),"). Every other file is listed, dot-prefixed ones included, because ",(0,r.jsx)(t.code,{children:'`include ".config.svh"'})," is legal and resolves."]}),"\n",(0,r.jsxs)(t.p,{children:["Both exclusions exist for one failure. Everything RTL Buddy writes into an artefact directory is written ",(0,r.jsx)(t.em,{children:"after"})," the fingerprint that would list it, so a listing containing any of it could never validate again: every later run saw a different listing and recompiled, and under ",(0,r.jsx)(t.code,{children:"--dispatch"})," that is every gated simulation job. Pruning the directory covers an ",(0,r.jsx)(t.code,{children:"+incdir+"})," that is an ",(0,r.jsx)(t.em,{children:"ancestor"})," of the artefact tree (",(0,r.jsx)(t.code,{children:"+incdir+."})," in a ",(0,r.jsx)(t.code,{children:"tests.yaml"}),", ",(0,r.jsx)(t.code,{children:"+incdir+.."})," from a design directory holding verif suites). Excluding the output names covers an include root that ",(0,r.jsx)(t.strong,{children:"is"})," an artefact directory: a ",(0,r.jsx)(t.code,{children:"preproc"})," hook may generate headers into its ",(0,r.jsx)(t.code,{children:"artifact_dir"}),", and the filelist then carries ",(0,r.jsx)(t.code,{children:"+incdir+artefacts/<test>"})," with no ",(0,r.jsx)(t.code,{children:"artefacts"})," component left for the walk to see. ",(0,r.jsxs)(t.strong,{children:["Generated headers under ",(0,r.jsx)(t.code,{children:"artefacts/"})," are tracked"]})," \u2014 they are real compile inputs and an edit to one rebuilds; only RTL Buddy's own outputs beside them are skipped."]}),"\n",(0,r.jsxs)(t.p,{children:["A simulator's own scratch output in the same directory is not excluded, because its spelling belongs to the tool rather than to RTL Buddy. Pointing an ",(0,r.jsx)(t.code,{children:"+incdir+"})," at a directory a builder writes into is therefore still a way to make every run recompile."]}),"\n",(0,r.jsxs)(t.p,{children:["What the listing does not cover: a directory that cannot be read is recorded as untracked, as it was before; a symlinked subdirectory under an ",(0,r.jsx)(t.code,{children:"+incdir+"})," is not descended, which bounds the walk against a link loop; and a header reached by any path no ",(0,r.jsx)(t.code,{children:"+incdir+"})," names is tracked only where a builder reports it \u2014 including the common case of an include resolved relative to the including file's own directory, which VCS and Verilator try before the search list, and which therefore stays untracked for the builders with no dependency file."]}),"\n",(0,r.jsxs)(t.p,{children:["Ambient environment variables and undeclared tool inputs are not tracked either. Force a compile with ",(0,r.jsx)(t.code,{children:"--rebuild"}),"; dropping ",(0,r.jsx)(t.code,{children:"--share-build"})," does not force one under ",(0,r.jsx)(t.code,{children:"--dispatch"}),", which implies it. ",(0,r.jsx)(t.code,{children:"compile.build_dep_changed"})," names a reported dependency that moved and ",(0,r.jsx)(t.code,{children:"compile.build_source_changed"})," a filelist entry, naming the file inside the directory where one is listed."]}),"\n",(0,r.jsxs)(t.p,{children:["Concurrent processes populating one shared directory are serialised by an advisory ",(0,r.jsx)(t.code,{children:"flock"})," on ",(0,r.jsx)(t.code,{children:"<shared directory>/.rb-build.lock"}),"; a process that has to wait logs ",(0,r.jsx)(t.code,{children:"compile.build_lock_wait"})," and repeats it every few minutes. Two bounds apply. On an NFS mount with ",(0,r.jsx)(t.code,{children:"nolock"}),", ",(0,r.jsx)(t.code,{children:"local_lock=flock"}),", or ",(0,r.jsx)(t.code,{children:"local_lock=all"}),", ",(0,r.jsx)(t.code,{children:"flock"})," is process-local and ",(0,r.jsx)(t.em,{children:"succeeds"}),", so there is no warning and the cross-node guarantee silently does not hold. And the lock file lives inside the directory it guards, so delete a shared build tree between runs, never during one \u2014 an ",(0,r.jsx)(t.code,{children:"rm -rf"})," that races a live run leaves the next process locking a fresh inode while the old holder still writes. Where the filesystem cannot lock at all, ",(0,r.jsx)(t.code,{children:"compile.build_lock_unavailable"})," is logged once per directory and the compile proceeds unserialised."]}),"\n",(0,r.jsxs)(t.p,{children:["Under ",(0,r.jsx)(t.code,{children:"--dispatch slurm"})," a second run of the same suite does not create that contention in the first place: each build job is named after the suite whose shared-build tree it writes and is submitted with ",(0,r.jsx)(t.code,{children:"--dependency=singleton"}),", so Slurm itself defers it until every earlier job of that name and owner has terminated. An interrupted run's orphaned build job is therefore waited on rather than raced; the waiting job then revalidates the shared build and reuses it if the inputs are unchanged. ",(0,r.jsx)(t.code,{children:"dispatch.build_job_deduped"})," names the job being waited on when the head's ",(0,r.jsx)(t.code,{children:"squeue"})," probe can see it \u2014 that probe only supplies the message, the guarantee does not need it, and a probe that fails is not retried for the rest of the run. Four bounds. The name covers the suite directory alone \u2014 not the planned tests, the builder mode, or the compile keys, all of which can differ between two runs that still write one ",(0,r.jsx)(t.code,{children:"obj_dir_<key>"})," \u2014 so an unrelated run of the same suite makes the second job wait for a build it then rebuilds, costing queue latency. ",(0,r.jsx)(t.code,{children:"singleton"})," is per user, so two ",(0,r.jsx)(t.em,{children:"users"})," building into one shared tree still meet at the lock. And a ",(0,r.jsx)(t.code,{children:"--dependency"})," of your own that uses the any-of separator ",(0,r.jsx)(t.code,{children:"?"})," cannot be composed with (Slurm permits one separator per expression), so the dedup stands down there and records ",(0,r.jsx)(t.code,{children:"dispatch.build_dedup_unavailable"})," at DEBUG \u2014 your gate is left exactly as it was. ",(0,r.jsx)(t.code,{children:"singleton"})," is also per cluster where a site sets ",(0,r.jsx)(t.code,{children:"DependencyParameters=disable_remote_singleton"}),": a federation normally resolves it across its clusters, but with that option two invocations routed to different clusters of one federation that shares this filesystem are not serialised against each other \u2014 pin a cluster with ",(0,r.jsx)(t.code,{children:"-M"}),", or rely on the ",(0,r.jsx)(t.code,{children:"flock"})," above. A multi-cluster ",(0,r.jsx)(t.code,{children:"sbatch-args"})," selection records that caveat once per run at DEBUG. A gate exported as ",(0,r.jsx)(t.code,{children:"SBATCH_DEPENDENCY"})," counts as yours too: it is folded into the same composition when ",(0,r.jsx)(t.code,{children:"sbatch-args"})," names no dependency of its own, so the build job's ",(0,r.jsx)(t.code,{children:"--dependency"})," never silently replaces it. Two ",(0,r.jsx)(t.code,{children:"sbatch-args"})," flags do not reach the build job for this reason: its ",(0,r.jsx)(t.code,{children:"--job-name"})," and ",(0,r.jsx)(t.code,{children:"--dependency"})," are emitted after your arguments, so you cannot rename a build job (simulation jobs are unaffected) and cannot replace its dependency, only add to it."]}),"\n",(0,r.jsxs)(t.p,{children:["A build job that stays ",(0,r.jsx)(t.code,{children:"PENDING"})," after ",(0,r.jsx)(t.code,{children:"dispatch.build_job_deduped"})," is usually waiting exactly as intended, so inspect the job ahead of it before cancelling anything: ",(0,r.jsx)(t.code,{children:"squeue -j <ids> -O JobID,State,Reason"})," on the ids that warning names, or ",(0,r.jsx)(t.code,{children:"squeue --name=<job name>"})," to find them (",(0,r.jsx)(t.code,{children:"dispatch.build_submitted"})," records the name), or ",(0,r.jsx)(t.code,{children:"scontrol show job <id>"}),". ",(0,r.jsx)(t.code,{children:"RUNNING"}),", or ",(0,r.jsx)(t.code,{children:"PENDING"})," with an ordinary capacity reason (",(0,r.jsx)(t.code,{children:"Resources"}),", ",(0,r.jsx)(t.code,{children:"Priority"}),"), means the wait ends when that build does \u2014 cancelling it would discard the build this run is about to reuse and kill the simulation jobs gated on it. Only a predecessor that will not finish deserves ",(0,r.jsx)(t.code,{children:"scancel"}),": held (",(0,r.jsx)(t.code,{children:"JobHeldUser"}),", ",(0,r.jsx)(t.code,{children:"JobHeldAdmin"}),"), unschedulable (",(0,r.jsx)(t.code,{children:"PartitionConfig"}),", ",(0,r.jsx)(t.code,{children:"BadConstraints"}),"), or an abandoned run you no longer want. Nothing times such a job out by default."]}),"\n",(0,r.jsxs)(t.p,{children:["Unshared builds are covered by neither mechanism. A suite where no planned test can share a build gets no build job at all (",(0,r.jsx)(t.code,{children:"dispatch.build_job_skipped"}),") and compiles inside each simulation job, into per-test artefact directories that take no build lock. Within one run those have a single writer, but two concurrent runs of such a suite write the same directories with nothing between them: do not run one twice at once. Interrupting a run also still leaves its jobs on the cluster and a re-run does not attach to them \u2014 the dedup makes the re-run's build job wait for the orphaned build, but the orphan's simulation jobs run to completion on their own."]}),"\n",(0,r.jsx)(t.h2,{id:"the-first-run-after-upgrading-recompiles-every-shared-build",children:"The first run after upgrading recompiles every shared build"}),"\n",(0,r.jsxs)(t.p,{children:["Build stamps hash content rather than comparing size and mtime, because a stat-only comparison can be answered from an NFS client's stale attribute cache: edit a source on the submit host, revalidate on a compute node inside that node's attribute-cache window, and ",(0,r.jsx)(t.code,{children:"stat"})," still describes the pre-edit file, so the stamp validates and the run reports a PASS for a design it never compiled. Reading content closes that, since close-to-open consistency revalidates on ",(0,r.jsx)(t.code,{children:"open()"}),". Stamps written by an earlier rtl_buddy carry no hashes, and ones written before include directories were listed are silent where a listing is now expected; neither can validate, so expect one rebuild per build directory after upgrading and none afterwards. A directory listing also changes the input digest a dispatched build job records beside a failed compile, so during a partial upgrade a gated simulation job can judge that compile's inputs to have moved and retry it once, even though it would fail the same way. That settles only once every host runs the new version: on a partially upgraded cluster \u2014 submit host upgraded, compute nodes not \u2014 an old node rewrites a hashless stamp that a new one then rejects, so builds keep recompiling until the upgrade reaches every node."]}),"\n",(0,r.jsxs)(t.p,{children:["Content decides from then on: regenerating a file byte-for-byte does not rebuild, and any real edit does. Reuse is also visible \u2014 ",(0,r.jsx)(t.code,{children:"compile.build_reused"})," names the reused directory and its stamp's age on the console, and the test's ",(0,r.jsx)(t.code,{children:"compile.log"})," repeats it with the command a rebuild would run, so an absent ",(0,r.jsx)(t.code,{children:"compile.log"})," is no longer the only hint. Use ",(0,r.jsx)(t.code,{children:"--rebuild"})," to compile regardless rather than deleting ",(0,r.jsx)(t.code,{children:"artefacts/.shared-builds/"})," by hand; if you do delete a tree, delete it between runs and not during one, for the locking reason above."]}),"\n",(0,r.jsxs)(t.h2,{id:"check-every-toplevel-before-upgrading-it-now-roots-the-compile",children:["Check every ",(0,r.jsx)(t.code,{children:"toplevel:"})," before upgrading: it now roots the compile"]}),"\n",(0,r.jsxs)(t.p,{children:[(0,r.jsx)(t.code,{children:"toplevel:"})," used to be inert for a plain SystemVerilog testbench \u2014 only the SystemC path, the cocotb path, and ",(0,r.jsx)(t.code,{children:"rb graph"})," / ",(0,r.jsx)(t.code,{children:"rb hier"}),' read it, and the schema described it as the "top-level DUT module name". It now reaches the builder as Verilator ',(0,r.jsx)(t.code,{children:"--top-module"}),", VCS ",(0,r.jsx)(t.code,{children:"-top"}),", or Icarus ",(0,r.jsx)(t.code,{children:"-s"}),", so it decides what the simulator elaborates."]}),"\n",(0,r.jsxs)(t.p,{children:["A project that took the old wording literally and pointed ",(0,r.jsx)(t.code,{children:"toplevel:"})," at the ",(0,r.jsx)(t.strong,{children:"DUT"})," rather than at the testbench therefore compiles a different design after upgrading, and it does so quietly. The observable symptom is a test that used to pass turning into ",(0,r.jsx)(t.code,{children:"NA"}),": the compile succeeds, the simulation runs and exits immediately because the elaborated root contains no ",(0,r.jsx)(t.code,{children:"initial"})," block, and the run ends with ",(0,r.jsx)(t.code,{children:"no PASS/FAIL markers found in .../test.log; result is NA"})," and ",(0,r.jsx)(t.code,{children:"test result unknown"})," in the summary. Nothing in that output names ",(0,r.jsx)(t.code,{children:"toplevel:"}),", so it is worth checking up front. Before upgrading, review every ",(0,r.jsx)(t.code,{children:"toplevel:"})," on a plain SystemVerilog testbench and confirm it names the module the simulator should elaborate \u2014 for an SV bench that is the bench itself, not the DUT it instantiates. A DUT with unconnected interface ports fails loudly instead (",(0,r.jsx)(t.code,{children:"%Error-UNSUPPORTED: Interfaced port on top level module"}),"), which is the same mistake with a better error. cocotb and SystemC testbenches need no review: for those, ",(0,r.jsx)(t.code,{children:"toplevel:"})," was already the elaboration root."]}),"\n",(0,r.jsxs)(t.p,{children:["Declaring ",(0,r.jsx)(t.code,{children:"toplevel:"})," also shifts the shared-build key once. The flag is part of the compile fingerprint, because it decides which modules are elaborated and what the model is called, so a testbench that gains a top flag gets a new shared-build directory on the first run after upgrading and compiles once more. That is every plain SystemVerilog testbench with a ",(0,r.jsx)(t.code,{children:"toplevel:"}),", plus cocotb on Verilator and Icarus. SystemC and cocotb-on-VCS keys are unchanged: those paths already passed their own top flag, so the plumbing recognises it and adds nothing. Testbenches without a ",(0,r.jsx)(t.code,{children:"toplevel:"})," are unchanged too \u2014 the top is not inferred from the testbench ",(0,r.jsx)(t.code,{children:"name"}),", so those builds still elect a top from filelist order and hash exactly as before."]}),"\n",(0,r.jsxs)(t.p,{children:["A top pinned in the builder's ",(0,r.jsx)(t.code,{children:"compile-time"})," opts continues to win, in every spelling the family accepts: Verilator takes ",(0,r.jsx)(t.code,{children:"--top-module"}),", ",(0,r.jsx)(t.code,{children:"-top-module"}),", ",(0,r.jsx)(t.code,{children:"--top"}),", and ",(0,r.jsx)(t.code,{children:"-top"}),", and Icarus accepts the module glued to the flag (",(0,r.jsx)(t.code,{children:"-stb"}),"). When the configured top names a different module than ",(0,r.jsx)(t.code,{children:"toplevel:"}),", the run logs ",(0,r.jsx)(t.code,{children:"compile.toplevel_conflict"})," once \u2014 naming both tops \u2014 and the configured top is used. That holds for SystemC and cocotb testbenches too: those backends generate their own top flag only when the builder pins none, so a configured top is never silently overridden by the generated one. Simulator families other than Verilator, VCS, and Icarus get no top flag at all; ",(0,r.jsx)(t.code,{children:"compile.toplevel_family_unsupported"})," records that at debug level."]}),"\n",(0,r.jsx)(t.h2,{id:"yosys-backed-flows-do-not-support-whitespace-in-paths",children:"Yosys-backed flows do not support whitespace in paths"}),"\n",(0,r.jsxs)(t.p,{children:["Yosys script parsing is not shell parsing: whitespace splits tokens, ",(0,r.jsx)(t.code,{children:"#"})," starts a comment, and single quotes from ",(0,r.jsx)(t.code,{children:"shlex.quote"})," do not group a path. Keep design and artifact paths for synthesis and FPV free of whitespace. ",(0,r.jsx)(t.code,{children:"fpv.yaml"})," parameter validation also rejects whitespace, ",(0,r.jsx)(t.code,{children:";"}),", and ",(0,r.jsx)(t.code,{children:"#"}),"."]}),"\n",(0,r.jsx)(t.p,{children:"String-valued parameter overrides require SystemVerilog quotes inside the YAML scalar; ordinary numeric values must not be quoted as strings."}),"\n",(0,r.jsx)(t.h2,{id:"static-lifetime-functions-corrupt-the-netlist-under-the-slang-frontend",children:"Static-lifetime functions corrupt the netlist under the slang frontend"}),"\n",(0,r.jsxs)(t.p,{children:["A ",(0,r.jsx)(t.code,{children:"function"})," or ",(0,r.jsx)(t.code,{children:"task"})," declared without ",(0,r.jsx)(t.code,{children:"automatic"})," outside a class has static lifetime, so all of its call sites share one storage location per formal. yosys-slang models this literally: two calls in one combinational process alias their arguments, and calls split across a combinational and a clocked process leave the shared net with conflicting drivers, which folds to ",(0,r.jsx)(t.code,{children:"x"})," and can drop a register and everything downstream. Simulation is unaffected, so the design can carry the defect indefinitely."]}),"\n",(0,r.jsxs)(t.p,{children:[(0,r.jsx)(t.code,{children:"rb synth"})," scans the filelist's sources, and the headers they ",(0,r.jsx)(t.code,{children:"`include"}),", before Yosys runs, and fails with ",(0,r.jsx)(t.code,{children:"frontend: slang"})," (",(0,r.jsx)(t.code,{children:"static-functions: error"}),") or warns with the legacy ",(0,r.jsx)(t.code,{children:"verilog"})," frontend, which inlines per call site. Add ",(0,r.jsx)(t.code,{children:"automatic"})," to the declaration. Yosys ",(0,r.jsx)(t.code,{children:"multiple conflicting drivers"})," warnings fail the run unless ",(0,r.jsx)(t.code,{children:"conflicting-drivers: allow"})," is set; a legitimate tristate bus produces the same warning and is not counted. Both gates cover the Yosys elaboration stage, so they apply to the ",(0,r.jsx)(t.code,{children:"yosys"})," and ",(0,r.jsx)(t.code,{children:"openroad"})," backends alike."]}),"\n",(0,r.jsxs)(t.p,{children:[(0,r.jsx)(t.strong,{children:"A slang run that passed before can now fail"}),", including one whose subroutines have a single call site and whose netlist happens to be correct: the scan reports the declaration, not the aliasing. The ",(0,r.jsx)(t.code,{children:"error"})," default is deliberate \u2014 the failure it guards is a silently corrupted netlist with plausible area and timing. Set ",(0,r.jsx)(t.code,{children:"static-functions: warn"})," to stage the migration; ",(0,r.jsx)(t.code,{children:"static_function_findings"})," then appears in the machine output of each affected run."]}),"\n",(0,r.jsxs)(t.p,{children:["The scan evaluates ",(0,r.jsx)(t.code,{children:"`ifdef"})," against exactly the macros Yosys receives: the synth.yaml entry's ",(0,r.jsx)(t.code,{children:"defines:"})," plus what the selected frontend predefines (",(0,r.jsx)(t.code,{children:"SYNTHESIS"})," and ",(0,r.jsx)(t.code,{children:"YOSYS"})," for ",(0,r.jsx)(t.code,{children:"read_verilog"}),"; ",(0,r.jsx)(t.code,{children:"SYNTHESIS"})," and slang's built-ins, but not ",(0,r.jsx)(t.code,{children:"YOSYS"}),", for ",(0,r.jsx)(t.code,{children:"read_slang"}),"). ",(0,r.jsx)(t.code,{children:"+define+"})," entries in the generated filelist are ",(0,r.jsx)(t.strong,{children:"not"})," applied by ",(0,r.jsx)(t.code,{children:"rb synth"})," \u2014 only the simulation flow acts on them \u2014 so the scan ignores them too and logs one ",(0,r.jsx)(t.code,{children:"synth.filelist_defines_ignored"})," warning naming what was dropped, what synthesis elaborates with a different value than the filelist asks for, and any bare ",(0,r.jsx)(t.code,{children:"+define+X"})," \u2014 valueless macros expand to an empty body under Verilator and ",(0,r.jsx)(t.code,{children:"read_verilog"})," but to ",(0,r.jsx)(t.code,{children:"1"})," under Icarus and slang, so they cannot be compared and are always reported. Move such a macro to ",(0,r.jsx)(t.code,{children:"defines:"})," if synthesis needs it; the underlying divergence between the simulation and synthesis flows predates this gate and is tracked separately. ",(0,r.jsx)(t.code,{children:"`undefineall"})," is honoured with the semantics of the frontend in use: slang re-applies the command-line macros after clearing, Yosys's ",(0,r.jsx)(t.code,{children:"read_verilog"})," does not, so the same source can leave a guarded region compiled under one frontend and not the other."]}),"\n",(0,r.jsxs)(t.p,{children:["The scan is a tokenizer with a definedness-only preprocessor, and it is imperfect in both directions. It misses declarations produced by macros (macro bodies are skipped at their ",(0,r.jsx)(t.code,{children:"`define"}),"), the contents of ",(0,r.jsx)(t.code,{children:"-y"})," library directories, and headers whose ",(0,r.jsx)(t.code,{children:"`include"})," cannot be resolved (logged at DEBUG). It can also report spuriously, because ",(0,r.jsx)(t.code,{children:"`if"})," expressions are not evaluated and scope nesting is tracked by keyword pairing rather than parsed. Add Verible's ",(0,r.jsx)(t.code,{children:"explicit-function-lifetime"})," rule through ",(0,r.jsx)(t.code,{children:"rb lint"})," and ",(0,r.jsx)(t.code,{children:"cfg-verible"})," to cover testbench and non-synthesisable sources. See ",(0,r.jsx)(t.a,{href:"/rtl_buddy/dev/concepts/synthesis#gate-static-lifetime-subroutines",children:"Synthesis"}),"."]}),"\n",(0,r.jsx)(t.h2,{id:"unknown-synthesis-overrides-are-ignored-after-a-warning",children:"Unknown synthesis overrides are ignored after a warning"}),"\n",(0,r.jsxs)(t.p,{children:[(0,r.jsx)(t.code,{children:"synth.yaml"})," ",(0,r.jsx)(t.code,{children:"tool_overrides"})," uses snake_case keys such as ",(0,r.jsx)(t.code,{children:"plugin_path"})," and ",(0,r.jsx)(t.code,{children:"single_unit"}),", unlike the kebab-case names under ",(0,r.jsx)(t.code,{children:"cfg-synth-tools.opts"}),". An unknown key logs ",(0,r.jsx)(t.code,{children:"synth_tool_config.unknown_override"})," and the run uses the default. A non-mapping override block or non-boolean ",(0,r.jsx)(t.code,{children:"single_unit"})," is fatal. See ",(0,r.jsx)(t.a,{href:"/rtl_buddy/dev/concepts/synthesis",children:"Synthesis"}),"."]}),"\n",(0,r.jsx)(t.h2,{id:"fpv-coi-analysis-is-best-effort",children:"FPV COI analysis is best-effort"}),"\n",(0,r.jsxs)(t.p,{children:["A cone-of-influence Yosys failure logs ",(0,r.jsx)(t.code,{children:"fpv coi_yosys_failed"}),", omits COI data, and does not fail a successful proof. If COI numbers disappear, inspect ",(0,r.jsx)(t.code,{children:"artefacts/<name>/coi.log"})," and verify ",(0,r.jsx)(t.code,{children:"cfg-fpv-tools[].opts.plugin-path"})," or ",(0,r.jsx)(t.code,{children:"RTL_BUDDY_SLANG_PLUGIN"}),"."]}),"\n",(0,r.jsx)(t.h2,{id:"fpga-bitstream-generation-relaxes-two-io-drcs",children:"FPGA bitstream generation relaxes two I/O DRCs"}),"\n",(0,r.jsxs)(t.p,{children:["Before ",(0,r.jsx)(t.code,{children:"write_bitstream"}),", rtl_buddy downgrades Vivado NSTD-1 and UCIO-1 so bring-up designs without a complete pinout can produce a bitstream. The earlier DRC report and machine result retain their original severity. Treat either violation as blocking for real hardware and add the missing ",(0,r.jsx)(t.code,{children:"IOSTANDARD"})," and ",(0,r.jsx)(t.code,{children:"LOC"})," constraints."]}),"\n",(0,r.jsx)(t.h2,{id:"fpga-timing-is-optional-unless-gated",children:"FPGA timing is optional unless gated"}),"\n",(0,r.jsxs)(t.p,{children:["A completed routed run reports PASS even with negative slack. Read ",(0,r.jsx)(t.code,{children:"timing_met"}),", ",(0,r.jsx)(t.code,{children:"wns_ns"}),", and ",(0,r.jsx)(t.code,{children:"failing_paths"})," for closure work. Set ",(0,r.jsx)(t.code,{children:"require-timing-met: true"})," in ",(0,r.jsx)(t.code,{children:"fpga.yaml"})," to make a reported miss fail the run; an unsupported ",(0,r.jsx)(t.code,{children:"timing_met: null"})," cannot trigger the gate."]}),"\n",(0,r.jsx)(t.h2,{id:"graph-coverage-source-changes-attribution",children:"Graph coverage source changes attribution"}),"\n",(0,r.jsxs)(t.p,{children:["A merged LCOV ",(0,r.jsx)(t.code,{children:".info"})," attributes coverage by file, so every module declared in one file receives the same totals. Use the default ",(0,r.jsx)(t.code,{children:"--coverage auto"})," or model artifacts for per-module attribution. Unresolved and re-anchored LCOV paths are reported in the summary."]}),"\n",(0,r.jsxs)(t.p,{children:[(0,r.jsx)(t.code,{children:"--coverage"})," requires ",(0,r.jsx)(t.code,{children:"auto"}),", ",(0,r.jsx)(t.code,{children:"model"}),", ",(0,r.jsx)(t.code,{children:"none"}),", or a merged ",(0,r.jsx)(t.code,{children:".info"})," path. ",(0,r.jsx)(t.code,{children:"--no-coverage"})," disables the join."]}),"\n",(0,r.jsx)(t.h2,{id:"a-cocotb-test-over-an-opted-out-model-keeps-a-dangling-dut-edge",children:"A cocotb test over an opted-out model keeps a dangling DUT edge"}),"\n",(0,r.jsxs)(t.p,{children:[(0,r.jsx)(t.code,{children:"models.yaml"})," ",(0,r.jsx)(t.code,{children:"graph: false"})," withdraws every config-tier edge into the model's hierarchy, but the binding tier's cocotb hop ",(0,r.jsx)(t.code,{children:"python_module --binds_to--\x3e module:<toplevel>"})," is derived from the merged graph and still names the DUT. That id stays in ",(0,r.jsx)(t.code,{children:"graph-meta.json"}),"'s ",(0,r.jsx)(t.code,{children:"merge.dangling"})," list, exactly as it does under ",(0,r.jsx)(t.code,{children:"--no-design"}),". Opt out only models that no cocotb test runs against, or give the model a ",(0,r.jsx)(t.code,{children:"top:"})," instead."]}),"\n",(0,r.jsx)(t.h2,{id:"the-viewer-distribution-and-executable-have-different-names",children:"The viewer distribution and executable have different names"}),"\n",(0,r.jsxs)(t.p,{children:["Install the ",(0,r.jsx)(t.code,{children:"rtl-buddy-sch"})," distribution; rtl_buddy invokes its ",(0,r.jsx)(t.code,{children:"rtl-buddy-view"})," executable and imports ",(0,r.jsx)(t.code,{children:"rtl_buddy_view"}),":"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-bash",children:"uv tool install rtl-buddy-sch\n"})}),"\n",(0,r.jsxs)(t.p,{children:[(0,r.jsx)(t.code,{children:"rb tool-check --explain rtl-buddy-sch"})," accepts the alias but reports the canonical tool key ",(0,r.jsx)(t.code,{children:"rtl-buddy-view"}),"."]}),"\n",(0,r.jsx)(t.h2,{id:"verible-lint-findings-are-on-stderr",children:"Verible lint findings are on stderr"}),"\n",(0,r.jsxs)(t.p,{children:[(0,r.jsx)(t.code,{children:"verible-verilog-lint"})," writes findings to stderr and uses its exit code for clean versus findings. A pipeline that reads only stdout sees nothing; capture stderr or use ",(0,r.jsx)(t.code,{children:"rb lint"}),", which scans both streams."]})]})}function h(e={}){const{wrapper:t}={...(0,i.R)(),...e.components};return t?(0,r.jsx)(t,{...e,children:(0,r.jsx)(l,{...e})}):l(e)}},8453(e,t,s){s.d(t,{R:()=>o,x:()=>a});var n=s(6540);const r={},i=n.createContext(r);function o(e){const t=n.useContext(i);return n.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function a(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(r):e.components||r:o(e.components),n.createElement(i.Provider,{value:t},e.children)}}}]);
## Set regression defaults

`cfg-rtl-reg` supplies fallback paths for simulation and flow regression manifests. An explicit `-c` option and a matching manifest in the invocation directory take precedence. See [Regressions](https://rtl-buddy.github.io/rtl_buddy/dev/concepts/regressions/#resolve-the-manifest).

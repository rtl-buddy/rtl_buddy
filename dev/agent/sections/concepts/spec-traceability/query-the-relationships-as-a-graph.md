## Query the relationships as a graph

The [design knowledge graph](https://rtl-buddy.github.io/rtl_buddy/dev/concepts/graph/) contains the same spec, model, test, and formal-run relationships. It uses the same loaders as `rb spec`, so graph queries and traceability checks share one interpretation of the YAML.

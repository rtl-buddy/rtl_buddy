## Run synthesis

```bash
uv run rb synth --list --synth-config path/to/synth.yaml
uv run rb synth smoke_synth --synth-config path/to/synth.yaml
```

The required backend and library configuration is covered in [Synthesis](https://rtl-buddy.github.io/rtl_buddy/dev/concepts/synthesis/).

## Environment Setup

Start with [Environment Setup](https://rtl-buddy.github.io/rtl_buddy/v5/development/setup/) for cloning, `uv sync`, pre-commit hooks, running the test suite, and building the wheel/sdist locally.
